# Eval Datasets

`evals/datasets/` is reserved for future evaluation datasets, examples, fixtures, and references.

This phase does not populate the directory with speculative content. Add records here only when a later prompt introduces concrete evaluation inputs.
