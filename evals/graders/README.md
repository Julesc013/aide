# Eval Graders

`evals/graders/` is reserved for future grader definitions, routines, and supporting references.

This phase defines the control-plane shape only. Working grader implementations belong here only when a later prompt explicitly adds them.
