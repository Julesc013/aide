# Shared Diagnostics

`shared/diagnostics/` is the future home for reusable diagnostic generation, normalization, and remediation-shaping logic.

It should emit stable diagnostic objects that work the same way across `embedded`, `cli-bridge`, and `local-service`.
