# Shared Transforms

`shared/transforms/` is the future home for reusable document, selection, workspace, and artifact transforms.

Logic here should operate on stable shared contract objects rather than host-native editor or project models.
