# Shared Core

`shared/core/` is the future home for reusable feature orchestration and business logic that is independent of any one IDE host.

Modules here should consume stable protocol objects and capability reports. They should not bind directly to host SDK objects or packaging rules.
