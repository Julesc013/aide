# Shared Config

`shared/config/` is the future home for shared settings resolution, override handling, and configuration-loading logic.

This area should own cross-host settings semantics, not host-specific preference UI or installation-specific configuration glue.
