# Shared CLI

`shared/cli/` is the future home for the shared command-line surface used by `cli-bridge` execution.

This area should wrap shared-core features behind stable request and response payload handling rather than host-specific command parsing.
