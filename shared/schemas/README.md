# Shared Schemas

`shared/schemas/` contains durable schema placeholders for the shared-core contract objects.

These schemas exist to stabilize data shape before implementation grows. They are implementation-support artifacts, not runtime code, and they must stay aligned with `specs/architecture/`.
