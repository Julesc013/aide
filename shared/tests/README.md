# Shared-Core Tests

`shared/tests/` contains the deterministic standard-library test suite for the shared-core bootstrap runtime.

The tests use JSON fixtures under `fixtures/boot-slice/` and verify runtime dispatch, structured diagnostics, capability reporting, and the host-agnostic CLI bridge.
