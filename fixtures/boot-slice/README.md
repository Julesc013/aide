# Boot-Slice Fixtures

`fixtures/boot-slice/` contains deterministic JSON requests and expected responses for the shared-core bootstrap runtime introduced in P10.

These fixtures are used by `shared/tests/` and by direct CLI smoke verification. They must stay human-readable, stable across runs, and free of host-machine-specific data.
