# AIDE Fixtures

`fixtures/` contains deterministic inputs and expected outputs used by evaluation and verification work.

The first committed fixture set lives under `fixtures/boot-slice/` and covers the shared-core bootstrap runtime introduced in P10. Fixtures in this repository must stay human-readable, deterministic, and free of machine-local or time-dependent data.
