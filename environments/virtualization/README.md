# Virtualization Environments

`environments/virtualization/` is for VM-oriented environment definitions, hypervisor notes, and related bring-up references.

This area is for metadata and guidance, not for storing heavyweight VM images in Git.
