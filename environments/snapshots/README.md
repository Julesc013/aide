# Environment Snapshots

`environments/snapshots/` is for snapshot, image, and checkpoint organization guidance.

Snapshot records may point to heavyweight binary assets that live outside Git. This directory should primarily hold metadata, notes, and reference structure.
