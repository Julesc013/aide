# Emulation Environments

`environments/emulation/` is for emulator-based environment definitions and notes where historical or platform-specific bring-up requires emulation rather than standard virtualization.

This area should record reproducible references and blockers, not bundled emulator payloads or proprietary system images.
