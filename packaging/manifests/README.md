# Packaging Manifests

`packaging/manifests/` holds manifest placeholders, templates, and later package-definition material.

This area does not hold final packages. Add manifest files here only when later prompts define concrete manifest shapes.
