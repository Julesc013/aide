# VSIX Manifest Placeholders

`packaging/manifests/vsix/` is reserved for VSIX-oriented manifest placeholders and templates.

This phase creates the directory structure only. No working manifests or built artifacts are introduced here.
