# Companion Manifest Placeholders

`packaging/manifests/companion/` is reserved for companion-lane packaging manifest placeholders and templates.

Companion release shape may differ materially from native-extension lanes, so the placeholders are kept separate.
