# AIDE Custom Agent Roles

`.codex/agents/` contains minimal role description files.

The role set is intentionally small. These roles exist for explicit delegation
of bounded work, not for always-on parallelism or broad architectural
authorship.

Role files must not pin model, provider, reasoning effort, approval policy,
sandbox posture, service tier, or search mode. Those remain user/session
choices.
