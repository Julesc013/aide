# AIDE Custom Agent Roles

`.codex/agents/` contains minimal role config files referenced by the repo-local Codex config.

The role set is intentionally small. These roles exist for explicit delegation of bounded work, not for always-on parallelism or broad architectural authorship.
