# Visual Studio For Mac Companion Lane

This directory is the `companion` compatibility-technology lane for `microsoft.visual-studio-mac`. It is an AIDE fallback for adjacent or archival workflows around a retired host family, not a literal Microsoft extensibility product name. Exact version coverage is tracked in inventory and matrices. Implementation is deferred to later prompts.
