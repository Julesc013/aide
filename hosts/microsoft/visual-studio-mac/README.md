# Microsoft Visual Studio For Mac

This directory is the archival host-family scaffold for Microsoft Visual Studio for Mac. Child directories represent the MonoDevelop-derived native lane and an AIDE companion fallback lane. Exact version coverage and retirement posture are tracked in inventory, matrices, and `research/microsoft/`.
