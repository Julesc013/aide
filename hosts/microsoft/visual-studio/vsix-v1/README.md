# Visual Studio VSIX v1 Lane

This directory is the `vsix-v1` compatibility-technology lane for `microsoft.visual-studio`. It represents the initial Visual Studio 2010-era VSIX packaging family. Exact version coverage is tracked in inventory and matrices. Implementation is deferred to later prompts.
