# Visual Studio VSIX v1 Lane

This directory is the `vsix-v1` compatibility-technology lane for `microsoft.visual-studio`. Exact version coverage is tracked in inventory and matrices. Implementation is deferred to later prompts.
