# Visual Studio COM Add-in Lane

This directory is the `com-addin` compatibility-technology lane for `microsoft.visual-studio`. In this repository it is a conservative umbrella for pre-VSIX legacy native Visual Studio extensibility rather than a full historical taxonomy. Exact version coverage is tracked in inventory and matrices. Implementation is deferred to later prompts.
