# Microsoft Visual Studio

This directory is the host-family scaffold for Microsoft Visual Studio. Child directories are compatibility-technology lanes rather than version buckets. Exact version coverage is tracked in inventory and matrices.
