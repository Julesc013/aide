# Visual Studio Extensibility Lane

This directory is the `extensibility` compatibility-technology lane for `microsoft.visual-studio`. It represents the modern VisualStudio.Extensibility SDK and related current contracts for late Visual Studio 2022 and Visual Studio 2026-era work. Exact version coverage is tracked in inventory and matrices. Implementation is deferred to later prompts.
