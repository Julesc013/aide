# Visual Studio Extensibility Lane

This directory is the `extensibility` compatibility-technology lane for `microsoft.visual-studio`. Exact version coverage is tracked in inventory and matrices. Implementation is deferred to later prompts.
