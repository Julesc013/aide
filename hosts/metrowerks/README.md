# Metrowerks Host Families

This subtree holds committed Metrowerks host-family scaffolds and their compatibility-technology lanes. It remains scoped to CodeWarrior.

P13 adds the first CodeWarrior boot-slice proof set:

- `ide-sdk` now has a runnable archival-native `cli-bridge` proof plus native-adjacent plug-in target metadata
- `companion` now has a runnable `cli-bridge` fallback proof for unresolved or non-native archival workflows

Exact version coverage and platform reach live in inventory and matrices, not in folder names.
