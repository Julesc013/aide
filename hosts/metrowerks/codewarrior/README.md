# Metrowerks CodeWarrior

This directory is the committed host-family scaffold for Metrowerks CodeWarrior. Child directories represent the documented native `ide-sdk` lane and the AIDE `companion` fallback lane. Exact version coverage and platform reach are tracked in inventory and matrices.
