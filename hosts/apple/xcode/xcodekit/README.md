# Xcode XcodeKit Lane

This directory is the `xcodekit` compatibility-technology lane for `apple.xcode`.

It represents Apple's documented Xcode Source Editor extension surface through XcodeKit. Exact version coverage is tracked in inventory and matrices. Implementation remains deferred to later prompts.
