# Apple Xcode

This directory is the host-family scaffold for Apple Xcode. Child directories are compatibility-technology lanes or companion lanes. Exact version coverage is tracked outside the path structure.
