# Core Tests

## Purpose

Core tests will eventually hold cross-Core tests for Contract, Harness, Runtime, Compatibility, Control, SDK, and bridge boundaries.

## Boundary

Q02 creates this skeleton only. Existing tests remain where they are, especially `shared/tests/**`, until a later reviewed migration preserves imports, fixtures, and evidence.
