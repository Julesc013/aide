# Eval Declaration Shape

## Purpose

Eval declarations describe self-hosting checks expected for the Profile/Contract.

## Required Fields

- `id`
- `purpose`
- `status`
- `owner_component`
- `future_harness`

## Boundary

Q03 declares eval intent only. It does not add executable eval runners or replace bootstrap-era eval records under `evals/**`.
