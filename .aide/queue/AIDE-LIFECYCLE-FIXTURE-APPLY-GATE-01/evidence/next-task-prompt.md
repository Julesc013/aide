# Next Task Prompt

Create `AIDE-LIFECYCLE-FIXTURE-INSTALL-MANAGED-SECTION-APPLY-01` only if fixture apply execution is explicitly authorized by that future WorkUnit. Scope it to the `install-managed-section` fixture, a single `manual/with-managed-section.md` managed-section update, explicit preimage and postimage hashes, rollback-compatible record capture, no active repo apply, no target repo mutation, no branch/worktree mutation, no provider/model/Gateway calls, no network calls, and a required independent checkpoint.
