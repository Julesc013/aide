# Prompt

Create `AIDE-LIFECYCLE-FIXTURE-APPLY-GATE-01` as a planning-only fixture apply gate. Review scoped transaction executor readiness, lifecycle proof closure, expected-report gap repair, install-managed-section generated plan, expected report, rollback record, hashes, and path boundaries.

Select the smallest future fixture apply proof if gate conditions are met. Do not execute fixture apply or lifecycle apply. Do not mutate fixture targets, generated plans, expected reports, implementation files, target repositories, branch/worktree state, release state, GitHub state, provider/model/Gateway surfaces, or network state.

Stop at `needs_review`.
