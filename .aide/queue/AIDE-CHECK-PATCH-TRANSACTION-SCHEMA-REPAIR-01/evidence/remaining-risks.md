# Remaining Risks

Remaining risks are warning-class and do not reopen the repaired material
path-scope defects:

- no accepted case-folding collision policy;
- no full JSON Schema implementation;
- no general diff parser;
- no artifact resolver or VCS reachability check;
- no policy, approval, apply, rollback, runtime, adapter execution, admission,
  or trust engine;
- inherited report, generated-output, OKF, and queue readability warning debt.

Downstream recovery must continue through explicit resume tasks so the original
blocked records remain historical.
