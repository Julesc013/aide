# Prompt

Create and process `AIDE-BUILD-DISTRIBUTION-PRODUCT-STATUS-PROJECTION-01`.

Repo truth outranks this prompt. Build a compact read-only distribution product-status projection from accepted queue/report evidence.

Stop at `needs_review`. Do not broaden distribution behavior, accept a new capability, perform real target apply, perform source repo self-apply, start canary inventory, generate or publish releases, implement package sources, call provider/model/network services, automate branches/worktrees, push, or mutate external repositories.
