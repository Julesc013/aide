# Warning Debt

Remaining warning debt:

- `distribution_product_status_v0` is built but not independently checked or accepted.
- ScreenSave, Eureka, Dominium, and Carbon canary profiles are not started.
- Local archive canary, public canary readiness, package-source verification, and shadow apply remain future tasks.
- Real target apply, source repo self-apply, provider/model/network calls, release publication, branch/worktree automation, and runtime behavior remain explicit non-capabilities.

These warnings are non-material for this build because the task is a read-only/report-only projection and does not broaden behavior.
