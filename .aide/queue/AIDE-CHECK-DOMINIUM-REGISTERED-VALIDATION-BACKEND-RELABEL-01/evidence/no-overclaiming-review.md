# No-Overclaiming Review

This check does not accept:

- aggregate validation success
- aggregate validation execution
- service-adapter entry
- universal read-only behavior beyond declared probe coverage
- general Dominium command execution
- generic registered-process provider
- runtime, worker, Service, Workbench, preview, apply, or rollback behavior

The observed Dominium semantic result remains:

```text
typed_refusal
```
