# Active Label Scan

Active registered-validation reports use:

```text
dominium_registered_validation_command_boundary_invocation_v0
```

The superseded label appears only under superseded or historical context:

```text
live_dominium_validation_command_readonly_v0
```

No active capability, status, projection, validation, or next-task surface uses
the old label as the current capability label.
