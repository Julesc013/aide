# Q40 Prompt Summary

Implement Root Recycling Framework v0.

Q40 consumes Q36 intent normalization, Q37 repo intelligence, Q38 quality
ledger evidence, and Q39 no-apply refactor control to create deterministic,
repo-local root inventory, root classification, file-fate, exception, risk,
and no-apply root recycling planning infrastructure.

The phase is planning-only. It must not move files, delete files, rewrite
references, apply move/salvage/path-alias maps, absorb tools, mutate branches,
mutate target repositories, call providers/models, use network calls, or
auto-fix source/docs/tests. It prepares Q41 Existing Tool Absorption v0.
