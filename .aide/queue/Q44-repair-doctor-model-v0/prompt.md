# Q44 Prompt Summary

Implement Repair / Doctor Model v0 in AIDE.

Q44 must detect broken, partial, stale, or inconsistent AIDE installs and
produce deterministic no-apply repair observations, diagnoses, plans, dry-run
reports, doctor reports, verification plans, policies, schemas, commands,
tests, golden tasks, docs, export-pack support, and evidence.

Q44 must not repair files automatically, overwrite target state, migrate files,
delete files, move files, rewrite references, mutate target repositories,
mutate branches, publish releases, activate CI, or call providers/models/network.
