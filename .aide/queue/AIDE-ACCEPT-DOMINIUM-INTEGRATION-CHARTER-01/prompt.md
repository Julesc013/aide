# Prompt

Create and process `AIDE-ACCEPT-DOMINIUM-INTEGRATION-CHARTER-01`.

Use `.aide/queue/index.yaml` as canonical AIDE queue truth. Re-read live repository state before writing. Accept or reject only the planning-only AIDE-Dominium integration charter after reviewing the complete source chain:

```text
charter build
-> independent remote freshness and semantic check
-> acceptance
```

Acceptance is limited to ownership, source hierarchy, namespaces, object mappings, command/result/refusal mappings, diagnostic/evidence/event mappings, transaction composition, Host/Bridge/Provider/Experience boundaries, Workbench non-authority law, compatibility, security, recovery, first read-only seam plan, first validation-slice plan, critical-path DAG, parallel read-only lane plan, turn-size policy, and milestone definitions.

Acceptance must not imply that cross-repository integration, Host Contract, Dominium Bridge, Workbench integration, runtime, service, command invocation, mutation preview, apply, rollback, or scene workflows exist.

Perform a bounded read-only remote Dominium freshness confirmation. Do not fetch, pull, merge, reset, rebase, update local remote refs, alter local Dominium files, create branches/worktrees, mutate GitHub, or modify Dominium.

If accepted, record planning-contract identity `aide_dominium_integration_charter_v0`, stop at `needs_review`, and recommend exactly `AIDE-BUILD-DOMINIUM-READONLY-SEAM-V0-01`.
