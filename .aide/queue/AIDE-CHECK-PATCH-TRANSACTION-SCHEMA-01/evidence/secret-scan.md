# Secret-Like Value Scan Evidence

Changed and new files scanned: `43`

Patterns checked:

- common `api_key`, `secret`, `token`, `password`, `passwd`, and
  `private_key` assignment shapes;
- AWS access key shape;
- PEM private key headers.

Findings: `0`
