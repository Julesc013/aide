# Reference Review Evidence

The transaction reference uses:

```text
aide://patch-transaction/synthetic-managed-section-review-candidate-01
```

Reviewed references include the transaction, WorkUnit, repository, target,
artifact, capability, ConformanceResult, EvidencePacket, and TestJob refs.

Wrong-kind and unknown required references fail closed according to focused
tests and independent report review. File paths are locators, not identity.

Result: `PASS`
