# AIDE-BUILD-DOMINIUM-REGISTERED-VALIDATION-BACKEND-RELABEL-01

Repair only the overclaiming active capability label and closely related
boundary projections from:

```text
AIDE-CHECK-DOMINIUM-REGISTERED-VALIDATION-BACKEND-01
```

Use this active label:

```text
dominium_registered_validation_command_boundary_invocation_v0
```

Do not alter execution behavior. Do not rerun the live Dominium command merely
to relabel active reports. Preserve historical evidence that contains the old
label.

Active reports must distinguish process start, launcher call count, structured
output parsing, registered command boundary, service-adapter boundary,
aggregate-validation execution, aggregate-validation success, typed domain
outcome, and mutation observed only within declared probe coverage.

Stop at `needs_review` and recommend exactly:

```text
AIDE-CHECK-DOMINIUM-REGISTERED-VALIDATION-BACKEND-RELABEL-01
```
