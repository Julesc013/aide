# Validation

The relabel task is validated by focused Python compilation, focused registered
validation backend tests, report-only registered-validation validation, task
inspect/evidence checks, active-label and leakage scans, broad AIDE validation,
diff checks, and commit policy.

The live Dominium command is not rerun for this task.
