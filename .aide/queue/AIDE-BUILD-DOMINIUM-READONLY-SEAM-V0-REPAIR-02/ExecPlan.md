# AIDE-BUILD-DOMINIUM-READONLY-SEAM-V0-REPAIR-02

## Objective

Repair the ten remaining material gaps from `AIDE-CHECK-DOMINIUM-READONLY-SEAM-V0-REPAIR-01` without broad rewrites or downstream implementation.

## Scope

The repair is limited to the existing offline read-only seam contracts, helpers, validators, conformance evidence, fixtures, tests, generated seam reports, Repair 02 reports, queue entry, and root planning/execution logs.

## Work Packages

1. Add complete diagnostic/refusal registry provenance disclosure.
2. Tighten public kind-specific schema and false status constraints.
3. Make negative fixture replay prove every expected error code and reject executable operations.
4. Replace aggregate conformance pass-through with assertion-specific evidence and aggregate-only `NOT_PROVEN` behavior.
5. Extend the operation ledger with allowed counts and operation-family instrumentation descriptions.
6. Add a runtime dependency manifest and isolated CLI portability proof.
7. Regenerate live seam outputs and write Repair 02 evidence.

## Verification

Run Python compilation, base seam tests, Repair 01 and Repair 02 regression tests, live seam project/validate/diff/demo, JSON parsing, secret-like scan, Dominium immutability check, broad AIDE validation, task inspect/evidence checks, and commit-policy validation.

## Exit Criteria

Stop at `needs_review` with `PASS_WITH_WARNINGS`, all ten gaps dispositioned as repaired pending independent check, no Dominium writes, no forbidden operation, complete reports/evidence, and exactly one next task recommendation: `AIDE-CHECK-DOMINIUM-READONLY-SEAM-V0-REPAIR-02`.
