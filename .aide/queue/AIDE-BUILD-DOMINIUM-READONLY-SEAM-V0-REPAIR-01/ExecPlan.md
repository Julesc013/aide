# AIDE-BUILD-DOMINIUM-READONLY-SEAM-V0-REPAIR-01

## Objective

Repair every material finding from `AIDE-CHECK-DOMINIUM-READONLY-SEAM-V0-01` while preserving the offline, deterministic, read-only seam boundary.

## Scope

The repair is limited to the Dominium seam contract, shared `core/interop/dominium` implementation, focused tests, fixtures, generated seam reports, repair reports, task evidence, queue index, and root planning/execution logs.

## Work Packages

1. Harden repository identity parsing and source snapshot binding.
2. Finalize source, record, projection-index, and bundle self-digests after validation metadata is embedded.
3. Preserve bounded diagnostic/refusal projection while disclosing native counts, projected counts, omitted counts, and omission digests.
4. Tighten the public schema and semantic validator for cardinality, required fields, ownership, references, capabilities, registries, events, and revisions.
5. Replace compact negative fixture descriptions with replayable operation fixtures.
6. Replace aggregate conformance propagation with expectation-level checks.
7. Make the demo timing claim truthful and add an operation observation ledger.
8. Add repair regression tests for the check findings.
9. Regenerate seam reports and write repair evidence.

## Verification

Run Python compilation, original seam tests, repair regression tests, live seam demo, live seam validation, JSON parsing, diff checks, Dominium immutability check, broad AIDE validation, task inspect/evidence checks, secret-like scan, and commit policy check.

## Exit Criteria

Stop at `needs_review` with `PASS_WITH_WARNINGS`, all 18 material findings dispositioned as repaired or covered by stronger validator evidence, no Dominium writes, no forbidden operation, complete reports/evidence, and exactly one next task recommendation: `AIDE-CHECK-DOMINIUM-READONLY-SEAM-V0-REPAIR-01`.
