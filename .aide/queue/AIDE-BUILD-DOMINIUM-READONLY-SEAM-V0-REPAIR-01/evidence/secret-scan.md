# Secret Scan

Final bounded value-oriented scan over the changed repair paths passed with no credential-like assignments.

Command:

```text
rg -n --hidden -S "(?i)(api[_-]?key|secret|token|password|private[_-]?key)\s*[:=]" <repair paths>
```

Result: no matches.
