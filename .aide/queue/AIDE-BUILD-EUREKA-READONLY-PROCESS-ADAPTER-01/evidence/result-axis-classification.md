# Result Axis Classification

Recorded shared outcome axes:

- `transport_outcome`: `transport_started`
- `process_outcome`: `exit_zero`
- `decoder_outcome`: `decoded`
- `domain_outcome`: `typed_result`
- `reason_code`: empty

The task keeps process transport, output decoding, domain validation, evidence,
and provider-acceptance status separate.
