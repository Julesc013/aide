# State Probe Coverage

Declared coverage:

- `git_revision`
- `git_porcelain_status`
- `git_tracked_tree_digest`
- `selected_command_digests`

Before and after state matched within this declared coverage. This is not a
claim about unobserved non-Git process side effects outside the probe.
