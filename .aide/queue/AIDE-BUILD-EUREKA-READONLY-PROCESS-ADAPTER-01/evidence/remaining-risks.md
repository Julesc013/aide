# Remaining Risks And Warnings

- `RegisteredProcessExecutionProvider v0` remains proposed and unaccepted.
- The selected local Eureka command is `scripts/public_alpha_smoke.py --json`,
  not the newer requested `scripts/validate_public_alpha_readonly.py --json`.
- The local Eureka checkout is behind `origin/main`; no fetch, pull, checkout, or
  branch operation was performed.
- Mutation observation is limited to declared Git probe coverage.
- No provider/model/network, worker, Service, Workbench, preview/apply/rollback,
  repository mutation, branch/worktree, GitHub, release, or promotion behavior
  was performed.
