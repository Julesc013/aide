# Remaining Risks

- The first result is evidence-projected, not runner-collected.
- The accepted profile lifecycle remains candidate and inactive.
- Warning debt from CapabilityManifest acceptance, ReferenceID/EventRecord
  predecessor compatibility, and latest-task-packet drift remains visible.
- Full JSON Schema Draft 2020-12 validation remains future work; the helper uses
  the repo's existing minimal schema subset plus semantic checks.
- Admission still requires a future acceptance gate and policy decision.

No blocker risk was found for this build slice.
