# Future Work

- `AIDE-CHECK-CONFORMANCE-RESULT-SCHEMA-01`: independently check schema, helper,
  projection, aggregation semantics, CLI, reports, and evidence.
- `AIDE-ACCEPT-CONFORMANCE-RESULT-SCHEMA-01`: accept the checked result slice if
  the check gate passes.
- Future conformance runner: collect live case observations only after protocol
  and admission semantics authorize it.
- Future admission gate: decide whether a result admits a subject only after
  accepted profile/result semantics exist.
- Future PatchTransaction: establish mutation semantics after admission
  foundation work is accepted.
