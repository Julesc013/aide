# AIDE-CONTINUE-00 Prompt

Run one bounded AIDE repository reconciliation phase.

Record the user-directed AIDE-only pivot in canonical queue truth. Defer target-repo work, preserve `X-TEST-01` as visible deferred target work, confirm `X-TEST-00` is implemented and review-gated, and make the latest task packet point to `X-OS-00 - AIDE Task OS Schemas and Policies`.

Do not implement Task OS. Do not mutate Eureka, Dominium, or any target repository. Do not run target commands. Do not apply install/repair/upgrade/rollback/uninstall behavior. Do not mutate branches, push, merge, promote, tag, publish, call GitHub APIs, call providers/models, or fetch network resources.

Write evidence, run validation, classify warnings, and stop at `needs_review`.
