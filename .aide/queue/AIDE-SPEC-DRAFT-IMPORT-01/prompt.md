# Prompt

Import the verified specs-only package into the live `specs/control-plane/`
tree now. Keep all imported chapters explicitly draft and proposed, preserve
the six adopted contracts exactly, exclude package tooling and private source
material, validate the resulting documentation, and publish one bounded commit.
