# Artifact Refresh Changed Files

The generator changed 26 tracked paths before task evidence was added:

- one export manifest under `.aide/export/aide-lite-pack-v0/`;
- eleven local bundle artifacts under `.aide/release/dist/`;
- ten preview-draft, checklist, upload-plan, and validation records under
  `.aide/release/`;
- four `latest-*` local release projections under `.aide/release/`.

No runtime source, test source, `.codex`, `.github`, user configuration,
credential, target repository, branch policy, tag, or remote release object was
changed by generation.
