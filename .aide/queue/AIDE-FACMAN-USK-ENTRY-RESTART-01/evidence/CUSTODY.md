# Entry replay evidence custody

`entry-replay-evidence.zip` and `entry-replay-custody.json` preserve exact provider and prior coordinator bytes with member SHA256 values. The ZIP is authoritative for raw evidence; text files checked out by Git may have equivalent line-ending projections.

Start with `provider/entry-restart-canonical-chain.json` for the directory checkpoint and `provider/zip-entry-replay-canonical-chain.json` for stored/Deflate ZIP replay. The latter binds PR42, PR43 and PR44, exact source tree, all three clean SDK gates and required hosted checks. `provider/zip-entry-root-independent-review.json` is ROOT's independent PASS, including the resolved bounded audit observation finding. Its source packet and complete patch are linked by exact hashes in the chain.

The archive also preserves earlier RED/intermediate records, native process-exit and Python/strict results, historical SDK failures, original reviewed source/binary custody, native commit-authority probes and their independent design review. `provider/child-commit-native-probe-source-provenance.json` explicitly distinguishes the initial retrospective stdin transcription from the later control probe sealed before execution.

Entry replay is integrated. Strong staged-child publication authority, generation/stale-owner leases, retained cleanup and consumer adoption remain pending. The next approved slice adds a stronger explicit requirement with no fallback and safe refusal where protected authority is unavailable. It will preserve a NEW actual post-verification same-byte publication reproduction; no earlier executed commit-window oracle is claimed.
