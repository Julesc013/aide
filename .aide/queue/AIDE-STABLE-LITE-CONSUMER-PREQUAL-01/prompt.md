# AIDE-STABLE-LITE-CONSUMER-PREQUAL-01

From the exact current local Lite preview archives, run disposable Windows
fresh and brownfield consumers for the mandatory installed workflow. Preserve
all failed attempts, script and log hashes, target-owned bytes, and unresolved
effect state. Classify real defects for a bounded repair WorkUnit. Independent
review must cover the exact canary and results. Final downloaded release
assets need a separate rerun; this task does not publish anything.
