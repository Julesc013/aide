# AIDE-ACCEPT-PROJECT-LOCK-V0-01

Accept exactly:

```text
project_lock_v0
```

Do not accept install truth, install plans, install/update/apply behavior,
admission, authorization, target repository mutation, release publication,
network/provider calls, Workbench/MCP runtime, OwnershipLedger v1, InstallRecord
v0, or downstream distribution behavior.

Stop at `needs_review` and recommend exactly:

```text
AIDE-BUILD-OWNERSHIP-LEDGER-V1-01
```
