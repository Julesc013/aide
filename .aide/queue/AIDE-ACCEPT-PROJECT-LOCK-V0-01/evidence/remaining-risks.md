# Remaining Risks

- ProjectLock v0 is accepted with warnings because it is a lock/selection object
  only.
- OwnershipLedger v1 is not built yet.
- Install truth, apply behavior, admission, authorization, target mutation,
  release publication, and runtime behavior remain non-capabilities.

The next serialized task is `AIDE-BUILD-OWNERSHIP-LEDGER-V1-01`.
