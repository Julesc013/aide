# Next Task Prompt

```text
Create and process:

AIDE-BUILD-OWNERSHIP-LEDGER-V1-01

Build OwnershipLedger v1 after accepted ProjectLock v0. The build must define
ownership taxonomy and ledger records for vendor-managed files, managed
sections, project-owned files, project overlays, generated target-local state,
runtime/local-only state, evidence-only state, preserved legacy state, unknown
ownership, and never-touch paths.

Do not implement install/update/apply behavior, target mutation, release
publication, network/provider calls, Workbench/MCP runtime, or downstream
InstallRecord/UpdatePlan objects.

Stop at needs_review and recommend exactly:

AIDE-CHECK-OWNERSHIP-LEDGER-V1-01
```
