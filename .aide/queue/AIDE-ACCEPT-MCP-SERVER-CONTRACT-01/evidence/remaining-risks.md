# Remaining Risks

No material acceptance blocker remains.

Accepted warning-class risks:

- full official MCP schema validation is not vendored or executed;
- MCP remains contract-only, projection-only, fixture-only, report-only, non-serving, non-callable, and non-networked;
- authorization remains expectation-only and does not implement OAuth, tokens, credentials, PolicyDecision, or CapabilityGrant enforcement;
- future `aide://interop` ReferenceID authority remains advisory;
- inherited report, generated-output, OKF, and Reconciler warning debt remains outside this acceptance.

The next serialized task is `AIDE-BUILD-A2A-AGENT-CARD-CONTRACT-01`.
