# Authorization Boundary Review

Accepted authorization boundary:

- MCP endpoint access authorization and AIDE operation authorization are distinct.
- Authorization expectations are declared.
- Authorization implementation is absent.
- No OAuth flow, token issuance, token parsing, token storage, credential resolution, PolicyDecision fabrication, or CapabilityGrant fabrication exists.
