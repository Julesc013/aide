# Warning Disposition

Accepted non-blocking warnings:

- No vendored full official MCP schema validation exists.
- No live MCP server, stdio transport, Streamable HTTP transport, endpoint, session, authorization, resource serving, prompt serving, tool execution, sampling, elicitation, MCP tasks, worker execution, provider/model/network call, runtime, or Service exists.
- Future `aide://interop` ReferenceID authority remains advisory and does not broaden accepted ReferenceID law.
- Inherited report, generated-output, OKF, and Reconciler warning debt remains outside this acceptance scope.

These warnings are non-blocking because the accepted capability is contract-only and fails closed for the repaired defects.
