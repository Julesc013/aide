# Next Task Prompt

Recommended next task:

```text
AIDE-BUILD-A2A-AGENT-CARD-CONTRACT-01
```

The next task should build only a bounded, contract-only A2A agent-card projection. It must not implement live A2A endpoints, authentication, task delegation, worker execution, provider/model/network calls, Host Contract, Dominium Bridge, Workbench, Runtime, Service, PatchTransaction apply, branch/worktree automation, GitHub mutation, release, promotion, or target-repository mutation.
