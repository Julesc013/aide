# Transport Expectation Review

Accepted transport expectation facts:

- Transport contract count: `2`.
- Transport profiles: `stdio`, `streamable_http`.
- Both are `contract_defined: true`.
- Both are `implementation_status: not_implemented`.

The contract records future expectations only. No stdio process, HTTP endpoint, SSE channel, session state, socket binding, or network transport exists.
