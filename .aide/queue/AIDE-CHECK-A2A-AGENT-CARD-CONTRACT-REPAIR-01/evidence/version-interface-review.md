# Version And Interface Review

The repaired contract pins:

- A2A specification release: `1.0.0`.
- A2A protocol version: `1.0`.

The projected non-live interface is:

```json
{
  "url": "https://aide.invalid/a2a/v1",
  "protocolBinding": "JSONRPC",
  "protocolVersion": "1.0"
}
```

The interface URL is a deliberately non-live `.invalid` fixture URL.
