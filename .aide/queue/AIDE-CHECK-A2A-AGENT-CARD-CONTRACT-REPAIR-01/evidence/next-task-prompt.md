# Next Task Prompt

# AIDE-ACCEPT-A2A-AGENT-CARD-CONTRACT-01
# Acceptance Review for Minimal A2A Agent Card Contract

Use `.aide/queue/index.yaml` as canonical queue truth. Re-read the live repository before writing anything.

Review the complete A2A build, failed independent check, bounded repair, and independent repair-check chain.

Accept only the minimal contract-only A2A Agent Card projection capability if all gates remain satisfied. Do not start an A2A endpoint, publish a card, register an agent, authenticate, authorize, delegate tasks, execute workers, call providers/models/network services, mutate repositories, implement Host Contract, implement Dominium Bridge, implement Workbench, implement Runtime or Service, release, promote, or claim production readiness.

If accepted, recommend `AIDE-DOMINIUM-INTEGRATION-CHARTER-01` or the live queue's next serialized task if the queue has advanced. If blocked, recommend one bounded repair or reconciliation task.
