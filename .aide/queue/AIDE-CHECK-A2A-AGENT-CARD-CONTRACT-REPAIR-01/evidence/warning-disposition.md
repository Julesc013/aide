# Warning Disposition

Non-blocking warnings:

- Full vendored official A2A JSON Schema validation remains deferred.
- A2A remains contract-only, fixture-only, projection-only, non-publishable, non-registered, non-delegating, and non-executable.
- No endpoint, discovery publication, registration, authentication, authorization, task delegation, worker execution, provider/model/network call, runtime, host integration, or mutation exists.

These warnings are not material because the repaired contract fails closed and does not overclaim live A2A behavior.
