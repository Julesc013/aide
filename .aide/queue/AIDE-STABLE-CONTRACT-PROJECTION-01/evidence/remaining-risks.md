# Remaining risks after contract preview dev integration

- The first stable Lite profile remains conditional. No final version/tag,
  support matrix, frozen release manifest, main promotion, uploaded asset,
  downloaded consumer or public publication has been qualified or performed.
- The local canary's successive V2/V3 packs are synthetic fixtures. Final
  declared-platform consumers must exercise actual frozen version pairs,
  explicit conflict decisions, interruption/restart, supported repair,
  rollback, removal, context/evidence and offline behavior from final bytes.
- `PASS_SOURCE_ANCESTOR` binds the preview archives to clean source
  `8365aa61`, not the later evidence/metadata `HEAD`. A final release freeze
  must bind exact source, assets, dependencies and reviewed bytes without
  rebuilding after qualification.
- The preview and draft retain `preview_only: true` and `no_publish: true`.
  Source/artifact/dev effect review does not qualify native, hosted, model,
  non-Windows apply or whole-product claims. Those profiles retain separate
  gates if later promised.
- The independent effect review conditioned this bounded result on an
  evidence-only task closeout. This file, `changed-files.md`, `validation.md`,
  and updated task/status/index/ExecPlan records are that proposed closeout;
  their exact commit must receive focused independent review before dev push.
