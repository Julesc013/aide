# Remaining Risks

- No adapter admission or trust exists.
- No adapter execution, worker launch, sandbox creation, credential resolution,
  provider/model/Gateway/network call, GitHub mutation, patch apply, target
  mutation, runtime, Service, Commander, Workbench, Test Broker, or ContextPack
  v2 behavior exists.
- Full JSON Schema Draft validation remains future work.
