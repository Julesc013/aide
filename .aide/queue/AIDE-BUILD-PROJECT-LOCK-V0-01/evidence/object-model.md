# Object Model

ProjectLock v0 contains an envelope, metadata, spec, status, and extension map. Metadata identifies the project lock and selected distribution. Spec records selected components, omitted optional components, overlays, compatibility range, install mode, warnings, features, non-capabilities, and extension data. Status records validation result and false implementation flags.
