# Digest Binding

ProjectLock v0 binds exact `selected_distribution_digest` and `manifest_payload_digest` from the accepted DistributionManifest. `project_lock_digest` excludes mutable status and treats channel as informational.
