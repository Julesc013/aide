# Component Selection

Required components are selected. Optional components must be explicitly selected or explicitly omitted. Selected component digests and artifact refs must match the accepted DistributionManifest.
