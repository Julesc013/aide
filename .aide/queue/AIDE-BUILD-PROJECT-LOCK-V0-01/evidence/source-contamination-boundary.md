# Source Contamination Boundary

ProjectLock rejects absolute paths, traversal paths, secret-like paths, `.aide.local`, source latest outputs, and source report refs in target-owned selection fields.
