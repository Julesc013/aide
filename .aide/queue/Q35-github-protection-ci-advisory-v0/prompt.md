# Q35 Prompt Summary

Implement GitHub Protection and CI Advisory v0 for AIDE.

The task resolves the QCHECK-03 blocker that Q35 was only planned. It must add
repo-local advisory policies, generated reports, AIDE Lite commands, tests,
golden tasks, docs, export-pack sync, and evidence.

The task must not call GitHub APIs, write `.github/workflows`, install CI,
apply branch protection, mutate branches, push, create tags, publish releases,
or call providers/models/network services.
