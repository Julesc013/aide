# Next Task Recommendation

Primary next task:

```text
AIDE-BUILD-EVIDENCE-PACKET-SCHEMA-01
```

Prompt seed:

```text
Create and process AIDE-BUILD-EVIDENCE-PACKET-SCHEMA-01. Extract the minimal EvidencePacket schema from existing lifecycle fixture runner evidence, contract-envelope projections, queue evidence, check reports, acceptance reports, and validation artifacts. Keep scope narrow: evidence identity, envelope metadata, producer, source task, checked capability, claims, explicit non-capabilities, validation commands, artifact/report references, hashes where already present, warnings, risks, compatibility metadata, and projection/validation helpers. Do not build WorkUnit schema, WorkUnit CLI, TestJob schema, Test Broker, Service, Commander, provider adapters, branch/worktree automation, target repo apply, active repo apply, rollback execution, release, promotion, network, Gateway, GitHub mutation, or model/provider calls. End at needs_review with evidence.
```
