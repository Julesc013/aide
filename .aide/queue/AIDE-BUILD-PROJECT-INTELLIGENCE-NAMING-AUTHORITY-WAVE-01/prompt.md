# AIDE-BUILD-PROJECT-INTELLIGENCE-NAMING-AUTHORITY-WAVE-01 Prompt

Build the first no-apply Project Intelligence naming and authority wave.

Cover `NamingPolicy`, `PathPolicy`, `RootPlacementRule`,
`FileKindRegistry`, `PathReservation`, `DirectoryContract`,
`NameDecision`, `PathDecision`, and `PathAuthorityRecord` candidates.

Do not enforce policy, move files, delete files, rewrite references, create
roots, apply aliases, mutate target repositories, or call providers/models.
