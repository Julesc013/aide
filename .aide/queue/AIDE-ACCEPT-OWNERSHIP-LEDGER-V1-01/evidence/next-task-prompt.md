# Next Task Prompt

```text
Create and process AIDE-BUILD-INSTALL-RECORD-V0-01.

Repo truth outranks this prompt. Inspect the live checkout before acting.

Build InstallRecord v0 after accepted OwnershipLedger v1.

Authority:
- Build only.
- Do not implement install apply, update apply, migration apply, repair apply,
  rollback apply, uninstall apply, target repository mutation, target scan
  authority, release publication, tags, uploads, GitHub Releases, network,
  provider/model calls, runtime, worker execution, Workbench, Commander,
  PreviewSession, DevelopmentTransaction apply, PatchTransaction apply, or
  branch/worktree automation.

Stop at needs_review and recommend exactly AIDE-CHECK-INSTALL-RECORD-V0-01.
```
