# Prompt: AIDE-CHECK-SELF-MANAGEMENT-CHARTER-01

Create and process a narrow check-only task for
`AIDE-BUILD-SELF-MANAGEMENT-CHARTER-01`.

Verify the self-management charter, boundaries, evidence, validation,
dirty-state classification, and next-task routing. Emit structured
GovernanceFinding records as a report convention only.

Do not implement schemas, CLI commands, GovernanceFinding helpers, OKF
regeneration, generated-output ledgers, doc truth reconcilers, file moves,
renames, reference rewrites, migration apply, runtime/provider/Gateway work,
GitHub/network work, branch/worktree automation, push/merge/release behavior,
or target-repo mutation.

Stop at `needs_review` with evidence.
