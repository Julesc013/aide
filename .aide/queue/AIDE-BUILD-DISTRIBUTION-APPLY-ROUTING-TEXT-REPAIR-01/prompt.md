# Prompt

Create and process `AIDE-BUILD-DISTRIBUTION-APPLY-ROUTING-TEXT-REPAIR-01`.

Repo truth outranks this prompt. Repair only stale operator-facing `distribution-apply status`, `distribution-apply plan`, and `distribution-apply verify` routing text after `aide_self_consumer_fixture_v0` acceptance.

Stop at `needs_review`. Do not broaden behavior, accept a new capability, perform real target apply, perform source repo self-apply, start canaries, generate or publish releases, implement package sources, call provider/model/network services, automate branches/worktrees, push, or mutate external repositories.
