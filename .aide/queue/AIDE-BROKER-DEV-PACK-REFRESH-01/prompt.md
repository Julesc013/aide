# Bounded Prompt

Refresh the tracked portable export pack and local release bundle from clean
post-broker-integration `dev`. Validate their exact provenance, checksums,
payload closure, extracted consumer behavior, and canonical repository state.

Do not modify runtime source or Codex configuration. Do not create or push a
tag, upload assets, publish a release, call GitHub mutation APIs, or alter a
target repository. Close the parent integration task only after the refreshed
artifacts and canonical validation pass.
