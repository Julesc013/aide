# Bounded source prompt

Implement explicit Windows recovery of a partially applied portable import
after an actual process exit. Bind exact pack, predecessor, plan, intent,
project controls, source and target bytes; refuse uncertain/rival states.
Preserve older intents as safe refusal and qualify new generated bytes in a
separate reviewed projection before any stable release claim.
