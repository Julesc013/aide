# Warning Disposition

Warnings are retained but non-blocking:

- MCP remains projection-only and contract-only.
- No live MCP server, transport, authorization, serving, invocation, or runtime
  exists.
- No full official MCP schema validator was added.
- Interop ReferenceID advisory warning debt remains unchanged.

These warnings do not indicate the two material fixture defects remain.
