# Authorization

On 2026-09-21 the owner directed `AIDE-CONVERGENCE-AND-DELIVERY-01` to:

- reconcile stale campaign admission and continuation fields;
- prepare an isolated documentation-only candidate against current `dev`;
- validate the actual combined documentation candidate;
- integrate the qualified candidate through the existing task-to-`dev` process;
- keep the mixed unfinished runtime branch out of that integration; and
- retain exact-candidate review for `main`, tags, releases, and publication.

This WorkUnit binds that direction to the paths, source and target refs,
validation requirements, exclusions, and retained gates in `task.yaml`.
