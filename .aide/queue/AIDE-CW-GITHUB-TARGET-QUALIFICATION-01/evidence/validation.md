# Validation status

Planning admission only. Settings/workflows/credentials and hosted effects are
unchanged. Source research and deterministic local graph experiments belong in
this task evidence and must remain distinct from hosted acceptance. The exact
configuration and independent target review remain pending.


Local graph experiments: 13 PASS in 25.986 seconds. The adjacent source, JSON
results and run receipt bind actual disposable Git object identities. They
model the documented predicates and expose missing-premise counterexamples;
they are not hosted GitHub acceptance. Raw response source is implemented under
the broker task with 26 offline tests; actual target observation/settings,
restricted principal, hosted workflow and adversarial server tests remain unrun.

Original graph stdout is retained as the exact target-qualification member
strict-contract-experiments.txt in the broker's github-observation-evidence.zip.
The adjacent script, structured result and run receipt remain directly
navigable; the broker custody map binds all original bytes after Git checkout.

## 2026-09-22 expected-head source slice

- PASS: `py -3 -B .aide/scripts/tests/test_continuous_worker_github_observation.py -v`
  ran 31 tests with zero failures or skips.
- PASS: fixtures cover fixed ordinary-merge request bytes, expected-head SHA,
  stale base/head/actor/policy/contract refusal, 405/409/422 refusal, bounded
  response parsing and submission-versus-integration separation.
- NOT RUN: live transport, credentials, settings observation/apply, workflow
  installation, hosted races and authoritative integration observation.

The source module has no network dispatcher. These results qualify local
request construction and response classification only.

Affected regression commands also passed:

- PASS: PR observation/staged broker, 21 tests.
- PASS: bounded GitHub HTTPS reader, 19 tests.
- PASS: registered provider bridge, 12 tests.
- PASS: 83 total affected cases, zero failures and zero skips.

The registered-bridge and staged-broker cases retain durable one-mutation
reservation, lost-reply uncertainty and no-replay behavior. The earlier broad
broker run's Windows symlink skip remains a separate explicit qualification gap;
these source tests neither reran nor discharged it.
