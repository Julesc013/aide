# Remaining qualification

No actual broker principal or current target policy is qualified. Strict checks
alone do not bind a PR target ref; policy drift, bypass, backward target movement,
check provenance and already-integrated candidates require explicit acceptance.
No real provider, protected Windows host or genuine two-task pilot is claimed.

The new fixed request makes GitHub's documented expected-head `sha` mandatory,
but the endpoint has no expected-base argument. Its pre-send base, actor, policy
and contract checks are not server-side atomicity. Exact active rules, bypass
visibility, restricted principal permissions, workflow provenance, protected
host/store and adversarial hosted races all remain open. A missing response is
uncertain and must be observed without replay through the existing durable
bridge contract.
