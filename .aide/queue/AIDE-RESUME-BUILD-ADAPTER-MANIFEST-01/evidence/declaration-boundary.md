# Declaration Boundary

AdapterManifest is a declaration, projection, validation, inspection, and
reporting slice only.

It does not start workers, run tests, create sandboxes, dispatch adapters,
resolve credentials, call providers, call network services, mutate GitHub,
create branches/worktrees, apply patches, or mutate target repositories.
