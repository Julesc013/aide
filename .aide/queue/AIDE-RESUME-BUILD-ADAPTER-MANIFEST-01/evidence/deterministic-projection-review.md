# Deterministic Projection Review

Focused tests run the AdapterManifest projection twice in a temporary workspace
and compare generated bytes for deterministic output. Source input bytes are
also compared before and after projection.

The projection report records `source_artifacts_mutated: false`.
