# Uninstall Dry-Run Review

## Result

`PASS_WITH_WARNINGS`

`uninstall-manual-preserved` is coherent but lacks a static expected report ref. `broad-delete-blocked` is coherent and explicitly blocked with `BLOCKED_BROAD_DELETE`. The warning does not block this checkpoint.
