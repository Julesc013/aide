# Authorization

The owner explicitly directed integration of the existing reviewed
repository-only neutrality checkpoint into `dev` before any main-promotion
decision. This authorizes the bounded task branch, exact candidate validation,
publication, and normal `dev` integration.

It does not authorize machine-wide configuration changes, permission widening,
`main` promotion, tags, releases, credentials, or unrelated runtime work.
