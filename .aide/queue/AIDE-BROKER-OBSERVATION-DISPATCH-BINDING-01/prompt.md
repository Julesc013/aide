# Prompt

Advance the existing broker by carrying the exact durable provider observation
into each registered mutation child and revalidating its operation/intent/digest
binding throughout execution. Adopt only the corresponding stage-effect and
observed-closeout requirements, add executable regressions, and keep all live
provider, credential, host, target, activation, and release claims false.
