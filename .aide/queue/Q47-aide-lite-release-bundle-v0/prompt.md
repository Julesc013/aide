# Q47 Prompt Summary

Implement AIDE Lite Release Bundle v0 in AIDE.

Q47 must generate local downloadable artifacts for the current
`aide-lite-pack-v0` export pack: `.zip`, `.tar.gz`, checksums, release manifest,
install notes, changelog and release-note preview copies, provenance, local
validation reports, fixture extraction validation, policies, schemas, commands,
tests, golden tasks, docs, export-pack integration, and evidence.

Q47 must not create Git tags, create GitHub Releases, upload artifacts, publish
packages, mutate branches, install CI, mutate target repositories, apply
install/repair/upgrade/rollback/uninstall behavior, or call providers, models,
or networks.
