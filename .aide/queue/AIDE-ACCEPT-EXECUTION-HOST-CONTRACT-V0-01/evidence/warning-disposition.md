# Warning Disposition

Accepted warnings:

- ExecutionHost contract v0 is projection-only.
- The check had reduced independence because it ran in the same Codex thread as the source build.
- Full external Draft 2020-12 validation was not separately installed.
- Python launcher selection differs when nested under Python on this host; check evidence used the active interpreter and scrubbed it.

Warnings are acceptable because this acceptance does not claim live host,
worker, runtime, provider/model/network, preview/apply, mutation, GitHub,
release, or promotion capability.
