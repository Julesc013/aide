# AIDE-BUILD-CANARY-PROFILE-MORE-INFINITE-RESEARCH-01 Prompt

Create and process `AIDE-BUILD-CANARY-PROFILE-MORE-INFINITE-RESEARCH-01`.

Build a read-only canary profile for `Julesc013/more-infinite-research` as an operator-prioritized downstream canary target before ScreenSave. If no local target path is available, use public metadata only and mark local target readiness partial or blocked. Do not mutate MIR, generate releases, publish, apply updates, create branches/worktrees, push, call providers/models, or broaden AIDE distribution behavior.

Expected result: `PASS_WITH_WARNINGS`, `PARTIAL`, or `BLOCKED`. If partial because a local checkout is missing, recommend `AIDE-BUILD-CANARY-PROFILE-MORE-INFINITE-RESEARCH-INPUTS-01`.
