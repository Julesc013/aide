# Prompt: AIDE-CHECK-REPORT-INDEX-01

Process Track B B1 closure wave 2 task 3.

Goal: independently verify the ReportIndex implementation, generated discovery
index, reports, evidence, boundaries, deterministic behavior, ambiguity
handling, provisional ledger dependency, and next-task routing.

Required constraints:

- Check only.
- No report repair, rewrite, move, rename, normalization, deletion, evidence
  rewrite, migration apply, runtime, provider, network, GitHub, branch/worktree,
  release, target-repo, Track A, or Track B B2 behavior.
- Validate the predecessor against its recorded baseline and record later
  current-HEAD self-observation separately.
- Stop at `needs_review` and recommend `AIDE-ACCEPT-REPORT-INDEX-01` on
  success.
