# Authority Finding

The check proves the observed capability as:

```text
dominium_registered_validation_command_boundary_readonly_v0
```

The source build currently proposes:

```text
live_dominium_validation_command_readonly_v0
```

That label is too broad for acceptance because the live Dominium command
returned a typed refusal:

```text
dominium.refusal.validation.tool_unavailable
```

The check therefore records one material finding:

```text
capability_label.overclaims_observed_boundary
```

The selected next task is:

```text
AIDE-BUILD-DOMINIUM-REGISTERED-VALIDATION-BACKEND-RELABEL-01
```
