# Compatibility Review

Compatibility preserved:

- existing CapabilityManifest helper and tests remain intact;
- existing predecessor validators remain callable;
- ReferenceID recognizes `conformance-profile` refs;
- profile reports use existing AIDE envelope and report patterns;
- CLI dispatch follows the existing `status/project/validate` shape.

No runtime, adapter, branch, target, release, GitHub, provider, Gateway, or
network behavior was introduced.
