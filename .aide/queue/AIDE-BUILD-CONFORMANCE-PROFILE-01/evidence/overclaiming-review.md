# Overclaiming Review

The generated profile and reports explicitly avoid claiming that a profile:

- proves a capability;
- admits a capability;
- executes checks;
- creates observed results;
- admits adapters;
- creates runtime authority;
- makes the repository production-ready or release-ready.

The helper scans generated Markdown reports for forbidden claim patterns. Current
validation reports `overclaiming_check_passed: true`.
