schema_version: aide.queue-status.v0
task_id: Q46-rollback-uninstall-model-v0
status: needs_review
updated: 2026-05-15
review_gate: needs_review
completed_steps:
  - baseline repo and Q45 install/repair/upgrade/export surfaces inspected
  - Q46 queue packet created
  - rollback and uninstall policies and safety gates created
  - rollback and uninstall schemas created
  - AIDE Lite rollback command family implemented
  - AIDE Lite uninstall command family implemented
  - generated rollback and uninstall no-apply outputs created
  - Q46 unit tests and golden tasks added
  - documentation and command catalog updated
  - export pack regenerated with rollback/uninstall support
  - validation run and evidence written
in_progress:
  - review gate
blockers: []
validation:
  state: complete
  notes:
    - Baseline Harness validate/doctor/self-check passed with the pre-existing stale generated manifest source fingerprint warning.
    - Baseline AIDE Lite doctor, validate, test, selftest, eval run, Q36-Q45 validations, verify, review-pack, ledger scan/report, pack-status, and commit check passed before Q46 edits.
    - Q45 status is needs_review and complete, with a recorded core/gateway tests timeout.
    - Baseline generated-report churn from eval/review/ledger commands was restored before Q46 edits.
    - Local main has remote divergence history; Q46 does not mutate branches or remotes.
    - Final Q46 rollback and uninstall validate/status/explain commands passed with no-apply, no overwrite, no delete, and no managed-section removal gates.
    - Final export pack checksums passed and excludes source-generated rollback/uninstall latest outputs as target truth.
    - Harness validate/doctor/self-check still report the pre-existing stale generated manifest warning.
    - core/gateway unittest discovery timed out after 900 seconds and no Q46 gateway edits were made.
