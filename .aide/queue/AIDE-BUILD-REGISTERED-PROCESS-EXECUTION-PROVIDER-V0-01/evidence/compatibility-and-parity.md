# Compatibility And Parity

Dominium parity preserved:

- one launcher call in focused fake-runner parity tests;
- `shell=False`;
- exact Dominium argv shape;
- typed refusal path preserved;
- registered command boundary remains `proven`;
- service-adapter boundary remains `unproven`;
- aggregate validation execution and success remain false;
- mutation observation remains `none_detected_within_probe_coverage`.

The live Dominium command was not rerun. Existing registered-validation reports
validate structurally after the adapter refactor.
