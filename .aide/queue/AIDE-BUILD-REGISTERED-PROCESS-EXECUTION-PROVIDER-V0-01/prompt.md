# Prompt

Build the generic RegisteredProcessExecutionProvider v0 after acceptance of:

```text
dominium_registered_validation_command_boundary_invocation_v0
```

The provider build must remain generic, stop at `needs_review`, and recommend
only:

```text
AIDE-CHECK-REGISTERED-PROCESS-EXECUTION-PROVIDER-V0-01
```

Do not accept the provider in this task.
