# Remaining Risks

- Acceptance is still required before ContextPack v2 is accepted.
- Source selection remains a minimal deterministic set, not a full context
  compiler or runtime context service.
- No source freshness policy, resolver, event store, secret-redaction engine,
  token budget enforcement, or runtime consumer exists.
