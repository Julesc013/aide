# Prompt

Remove the obsolete repository Codex configuration that forces model,
reasoning, provider, permission, sandbox, search, or service-tier choices.
Preserve minimal role descriptions, keep machine-wide settings outside this
WorkUnit, verify that active pins are absent, and commit the bounded result.
