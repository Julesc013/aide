# AIDE-CHECK-CONTRACT-ENVELOPE-01

Perform an independent check of `AIDE-BUILD-CONTRACT-ENVELOPE-01`.

Review the minimal contract envelope helper, schema, CLI dispatch, focused
tests, queue evidence, generated reports, projections, and backward
compatibility with the accepted lifecycle fixture runner reports.

Do not repair inline. Do not implement EvidencePacket schema, WorkUnit schema,
WorkUnit CLI, TestJob schema, Test Broker, Service, Commander, provider
adapters, branch/worktree automation, target repo apply, active repo apply,
rollback execution, release, promotion, network, Gateway, GitHub mutation, or
model/provider calls.

End at `needs_review`.
