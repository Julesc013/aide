# Prompt: AIDE-DOMINIUM-INTEGRATION-CHARTER-01

Create and process `AIDE-DOMINIUM-INTEGRATION-CHARTER-01` as a single planning-only queue item.

Use `.aide/queue/index.yaml` as canonical AIDE truth. Re-read live AIDE and Dominium repository state before writing. Repository artifacts outrank this prompt, generated next-task packets, prior chat, and remembered architecture.

Write only:

- `.aide/queue/AIDE-DOMINIUM-INTEGRATION-CHARTER-01/**`
- `.aide/reports/dominium-integration-charter/**`
- `.aide/queue/index.yaml`
- `PLANS.md`
- `IMPLEMENT.md`

Do not modify Dominium, Domino, Workbench source, sibling repositories, target repositories, GitHub, branches, worktrees, or remote refs.

The charter must freeze semantic ownership, source-of-truth hierarchy, namespace ownership, stable-reference mappings, command/result/refusal mappings, evidence and event mappings, transaction composition, host and bridge responsibilities, version compatibility, security and authority boundaries, failure and recovery law, first executable read-only seam, first validation slice, critical-path DAG, and parallel read-only RepoGraph lane.

Primary doctrine:

```text
Universal semantics at boundaries.
Domain meaning inside domains.
Host presentation inside hosts.
Specialized execution in hot paths.
```

No live endpoint, publication, registration, authentication, authorization, delegation, worker execution, provider/model/network call, runtime, Host Contract, Dominium Bridge implementation, Workbench implementation, repository mutation, release, or promotion.

Stop at `needs_review` and recommend only `AIDE-CHECK-DOMINIUM-INTEGRATION-CHARTER-01`.
