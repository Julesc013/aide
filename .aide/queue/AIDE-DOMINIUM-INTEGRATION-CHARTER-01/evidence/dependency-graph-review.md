# Dependency Graph Review

The graph covers charter closure, read-only seam, validation slice, read-only Workbench, local store/service, trust/invocation, preview, document preview/apply/rollback, scene preview/apply/undo, and read-only RepoGraph Track B2.

Downstream task IDs are planning graph nodes only. No downstream queue directories were materialized.
