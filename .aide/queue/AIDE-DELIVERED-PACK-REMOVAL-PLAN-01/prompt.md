# Prompt

Continue the admitted stable implementation campaign with one bounded product
slice: implement deterministic, receipt-backed removal planning in the existing
portable AIDE Lite CLI.

The command must run from extracted release bytes, validate the installed
receipt, compare exact current managed bytes, preserve authored and unknown
content, emit an exact plan digest, and perform no target mutation. Keep Q46
uninstall apply, live target use, main promotion, tags, upload, and publication
outside this WorkUnit.

