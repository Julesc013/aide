# Prompt

Create a schema and fixture-shape WorkUnit for lifecycle apply planning after `AIDE-APPLY-LIFECYCLE-PLAN-01`. Define lifecycle manifest, lifecycle plan, lifecycle report, rollback-compatible lifecycle record, fixture repository shape, scoped transaction executor interlock, validation design, token/quality ledger hook, evidence, and one safe next task.

Do not implement or execute lifecycle apply. Do not implement or execute install apply, upgrade apply, lifecycle repair apply, rollback, or uninstall. Do not mutate active AIDE repo files through scoped transaction apply, mutate target repositories, mutate branches/worktrees, merge, push, promote, publish releases, call GitHub, call providers/models, call Gateway, use network calls, or mark any lifecycle surface production-ready, release-ready, target-capable, autonomous, or broad-apply capable.
