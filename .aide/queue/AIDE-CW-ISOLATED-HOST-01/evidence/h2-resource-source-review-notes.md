# Frozen two-API resource feasibility source review

The three task-owned files are frozen at N1 checkpoint 2a44f17e. Existing
runtime and N1 source files are unchanged dependencies. Source aggregate and
complete new-file patch are defined by h2-resource-source-manifest.json.
All 88 injected tests pass: 21 new resource/controller/journal tests, 30 N1
and 37 observer regressions. All actual native constructors were patched to
fail during the test command. The only real input is existing archived N1
JSON/test evidence; no DLL input was reread, mapped, copied or granted.

The parent requires the byte-exact N1 custody archive and its three exact raw
members: old effect, controller stdout and independent actual audit. It derives
new observation pins from the strictly parsed eight-file result, using only a
fresh request/expiry. Effect-provided identities/hashes/descriptors/spelling must
match those derived facts. The archive has a 2 MiB byte ceiling, 128-member
ceiling and 64 KiB per selected member. No extraction or arbitrary archive path
is used. Ordinary source/ZIP hash pre-reads remain under the same explicitly
unqualified protected-controller namespace premise.

The parent flushes a fresh handle-relative request reservation and both exact
canonical mapping intents before child creation. Their newline-inclusive
journal hashes differ deliberately from ObservationSession's canonical intent
acknowledgements. The immutable transfer binds both forms. ParentPaidJournal
can acknowledge only that sequence once; any wrong/consumed request invalidates
its remaining ticket. These hashes attest to the reviewed parent code ordering;
they are not signatures or a new adversarial worker authority boundary.

The child admits exactly api-ms-win-core-path-l1-1-0.dll and
api-ms-win-crt-runtime-l1-1-0.dll, with the existing flags 0x860 and no fallback.
It uses the reviewed observer to hash/pin eight native files and hold all read
handles through mappings. Each resource handle is released exactly once. A tag0
return must already be in that process's pre-call executable inventory; tagged
resources and prior executable references remain distinguished. Mapped spelling
must match both the fresh held object and the archived N1 spelling, including
KernelBase.dll. Strict result parsing retains all qualification flags false.
A failed LoadLibraryExW records only the exact API name, fixed flags and numeric
Win32 error, without reflected exception text, secrets or automatic retry.

Bounds are unchanged: observer 8192 adapter calls and 2 MiB internal result cap;
this two-API wrapper caps transfer/stdout/stderr at 64 KiB, child duration at25
seconds and guard calls at32768. There are exactly two additional guarded native
environment observations outside the observer adapter count. The parent keeps
its existing 4096 verification-call and90-second successful-completion ceiling;
Job child timeout30 seconds,256MiB and process1, with separate existing cleanup
budgets. Those are distinct counters/time scopes, not a total30-second claim.

Two pre-paid parent intents are possible-dispatch evidence even if the child
fails before either mapping. Provisional journal observations are not final
controller success. Missing replies, expiry, false acknowledgements, uncertain
release and source drift remain consumed without automatic replay. The N1
reservation/generation and all H1 failure/success histories remain untouched.

No actual resource effect manifest has been prepared or admitted. The source
needs independent review before a new exact effect proposal. Full180-name API
inventory against128-row cap, contextual restricted-loader proof, OS trust,
private image/grants, controller/journal authenticity, credential host and worker
activation remain open. Installed ordinary Python/bootstrap and the same-user
parent process/handle namespace remain explicit trusted-probe premises.
