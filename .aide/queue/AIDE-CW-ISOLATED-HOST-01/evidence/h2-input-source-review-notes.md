# Frozen eight-DLL read-facts driver review

Source3 is task-owned and remains outside production worker wiring. The exact
manifest and complete new-file patch bind child, controller and injected tests.
All 67 selected tests pass: 30 new injected driver/controller/native-ABI tests
plus 37 unchanged injected observer regressions. First24-test evidence is retained.
No test constructs the actual read-only facade or starts a real child.

The separate effect manifest admits exactly one ordinary Python child, eight
named public file inputs totalling 8,306,752 bytes, no selected mappings and no
copy/grant/profile/network effects. Existing marker-owned H1 TEMP parent identity
and marker were reobserved read-only; no profile is opened/adopted/replayed. The
new generation's journal/output do not exist. Initial parent metadata and actual
interpreter/source bytes are the only real preparation observations in this slice.
The historical eight input bytes are read from the prior immutable receipt,
not reread from Windows. Finite expiry and the source pin map must still match
at dispatch. The source/effect packet itself does not grant effect approval.

The child exposes read-only ABI methods, requires native AMD64, exact system
root/local volume and eight historical byte pins. All root/file handles remain
held through hashing and final facts checks; each close is consumed once. A
release error or expiry/revoked guard after final cleanup refuses success.
System hardlinks are recorded and bounded to1024; private single-link rules are
unchanged. Owner/DACL observation is a bounded SHA256 of the native SDDL, with
owner SID and actual normalized native object path/identity. Raw SDDL is not
returned. No trustworthiness is inferred solely from those observed names/hashes.

The controller flushes an exclusive handle-relative intent before dispatch.
It rechecks finite authority/source/parent pins immediately before creation,
after suspended creation, after resume and during execution. Each controller
observation is bounded to4096, each child adapter call to4096, output to65536
bytes and each of8 inputs to16MiB. The owned Job has process limit1,256MiB memory
and30-second child execution timeout. Child internal elapsed bound is25seconds;
controller success elapsed ceiling is90seconds. Job quiescence/drain cleanup
has the existing independent bounds, so30seconds is not a total wall-time claim.
The controller validates every returned byte/identity row and permits no API,
loader, provenance or activation qualification. The journal's completed
observation remains provisional until final controller checks; failures and
missing replies stay consumed and cannot replay.

The ordinary interpreter EXE is byte-pinned. Its transitive installed stdlib,
DLLs and process bootstrap are existing tooling, not a qualified private image.
Source hash pre-reads and parent leases rely on the explicitly admitted protected
controller namespace; they do not freeze arbitrary same-user code, Win32 device
mappings or output path replacement. The new journal is handle-relative; existing
Job output path creation remains pathname-based under that premise. This probe
can establish actual read ABI/input facts only. Strong controller/journal
provenance, contextual API mapping, the full180-name inventory versus128cap,
private image grants and restricted Python loading remain open.
