# Next Task Routing

Recommended next task:

```text
AIDE-CHECK-INSTALL-RECORD-V0-01
```

Reason:

InstallRecord v0 is built with focused tests, fixtures, reports, and validation, but it remains proposed until an independent check verifies the build and its no-apply boundaries.
