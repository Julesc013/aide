# Validation Summary

InstallRecord v0 validates as `PASS_WITH_WARNINGS`.

The focused tests cover schema shape, predecessor binding, fail-closed semantic validation, optional extension preservation, fixture coverage, CLI output, and parser refusal of apply-like subcommands.

The helper-generated validation report is `.aide/reports/install-record-v0/validation.json`.
