# AIDE-CONVERGENCE-AND-DELIVERY-01

## Purpose

Coordinate a restartable convergence and delivery campaign without turning one
overbroad prompt into uncontrolled repository, Git, or release mutation.

## Scope

This programme packet owns admission, durable campaign state, child routing,
and compact evidence. It does not directly edit product code, specifications,
root documentation, branches, tags, releases, machine configuration, or target
repositories. Each executable slice requires its own bounded queue packet.

## Non-Goals

- Do not apply the private handoff or prior R3 overlays wholesale.
- Do not replace the existing broker, isolated-host, distribution, or queue machinery.
- Do not make every advanced profile a prerequisite to a bounded release.
- Do not merge unfinished branches merely to make the worktree clean.
- Do not restore Codex model, reasoning, provider, permission, sandbox, search,
  or service-tier pins.

## Current Facts

- Initial source HEAD: `2a44f17eb7232757133df549ac6fc519d535e71b`.
- The task branch is six commits ahead of its matching remote-tracking ref and
  zero behind; it has no configured upstream.
- Expanded inventory found 25 tracked dirty paths and 714 untracked files.
- No stash or active merge, rebase, cherry-pick, or revert marker was observed.
- A stale detached worktree registration points at an unavailable directory;
  commit `492faa4f1a8280ba67954aa4fc252e79f2e19c15` is preserved in the bundle.
- The private recovery snapshot reproduced the dirty status byte-for-byte.
- The private action pack verified 21 archives, 2,480 archive entries, 1,244
  obligation entries, and 2,003 source occurrences; semantic adoption remains false.
- The intent compiler classifies the combined request as destructive,
  split-required, and blocked as a single WorkUnit.

## Milestones

1. Admit this programme through the queue review gate.
2. Checkpoint only the intended repository `.codex` neutrality change.
3. Classify and checkpoint every initial dirty path and unique commit by owner.
4. Diagnose bounded `doctor` and `validate` behavior without indefinite retries.
5. Review and correct only the artificial requirements-intake dependency.
6. Reconcile exact source clauses into live desired-state specifications in
   coherent reviewed families, preserving proposals and implementation maturity.
7. Integrate qualified work into `dev` with prospective combined validation.
8. Close a bounded usable foundation profile and test the delivered artifact.
9. Prepare exact main-promotion and release candidates for their hard gates.
10. Publish only after exact source, tag, channel, assets, and approvals are bound.
11. Record final dispositions and durable continuation for advanced runtime work.

## Validation

- Validate task packet structure and queue indexing.
- Run `git diff --check` for the admission candidate.
- Re-run intent validation.
- Confirm the private pack and recovery snapshot receipts remain valid.
- Do not claim broad repository, behavioral, integration, or release validation
  from admission-only checks.

## Evidence

Admission evidence lives under this task's `evidence/` directory. Private source
archives and recovery bytes remain outside Git; repository evidence records only
their identities, verification result, and limitations.

## Progress

- [x] Action pack extracted outside the repository and verified.
- [x] Supplied utility source reviewed and fixture tests run.
- [x] Repository and registered worktrees inventoried.
- [x] Recoverable private snapshot created and restore-tested.
- [x] Campaign intent compiled and validated.
- [x] Exact programme admission candidate prepared.
- [ ] Human review accepts or rejects this queue admission candidate.
- [ ] First bounded child WorkUnit is admitted.

## Discoveries And Decisions

- Expanded status contains 739 individual dirty paths, not the earlier compact
  599 status entries; Git had collapsed untracked directories in the older count.
- The only restore mismatch in the first snapshot attempt was a working-tree
  line-ending representation. Exact tracked-file copies were added and the
  second restore reproduced status and file hashes.
- Queue admission is itself a hard review gate. Preparation may proceed to
  `needs_review`; implementation may not begin under this packet yet.
- Root planning and documentation files already contain unrelated dirty work,
  so admission does not edit them.

## Recovery And Resumption

Resume from this ExecPlan, `status.yaml`, and task evidence. Recheck HEAD, dirty
status digest, active writers, and policy before any child execution. Never use
the private snapshot as a silent rollback source; restoration is an explicit
operator recovery action.

## Retrospective

Admission was accepted by the owner against the exact candidate manifest.
Bounded child execution is active; no main promotion, tag, release, or public
publication has occurred.
