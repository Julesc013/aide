# Validation

Validation is recorded in:

- `validation-commands.md`
- `validation-results.md`

This task uses structural validation only. It does not execute providers,
workers, runtime services, network calls, Dominium commands, preview/apply,
rollback, target-repository mutation, branch/worktree mutation, GitHub mutation,
release, or promotion behavior.
