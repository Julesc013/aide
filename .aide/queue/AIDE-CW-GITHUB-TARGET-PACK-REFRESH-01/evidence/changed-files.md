# Generated path scope

- Preview checkpoint `66ef462c` changes five files under `.aide/changelog/**`.
- Artifact checkpoint `cda504b9` changes the export manifest and 27 local
  release files under `.aide/release/**`; the portable payload files and
  repaired generator remain unchanged.
- The first committed-source replay changes 18 release metadata/projection
  files when pack provenance advances to `PASS_SOURCE_ANCESTOR`.
- No runtime source, `.github/**`, `.codex/**`, main, target repository, or
  hosted setting was changed by artifact generation.
