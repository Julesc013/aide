# AIDE-ACCEPT-WORKUNIT-CLI-01

Acceptance review for the read-only WorkUnit CLI after:

- `AIDE-BUILD-WORKUNIT-CLI-01`
- `AIDE-CHECK-WORKUNIT-CLI-01`

This is a check-only task. Do not implement mutation CLI, runtime, scheduler, Test Broker, Service, Commander, provider adapters, branch/worktree automation, target apply, active apply, rollback execution, release, network, Gateway, GitHub mutation, or model/provider calls.

End with exactly one acceptance result and leave this task at `needs_review`.
