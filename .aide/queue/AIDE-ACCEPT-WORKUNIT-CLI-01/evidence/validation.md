# Validation

Passed: workunit CLI positive/negative/path-safety commands, predecessor compatibility commands, repo validate, repo test, task inspect/evidence commands, git diff --check HEAD^ HEAD, JSON report parse checks, YAML structural checks, refined overclaiming scan, refined secret scan.
