# Validation

Final validation is recorded in `validation-results.json` and
`validation-results.md`.

The passing command set includes schema JSON parsing, focused
DistributionManifest tests, DistributionManifest status/project/validate,
Q43-Q48 no-apply validators, release validation, release-draft validation, task
inspect/evidence, broad AIDE validation, JSON report parsing, path/secret scans,
and diff checks.
