# AIDE-CHECK-PATCH-TRANSACTION-SCHEMA-01

Independently check the minimal PatchTransaction schema slice created by
`AIDE-BUILD-PATCH-TRANSACTION-SCHEMA-01`.

Re-read live repository truth, verify the task packet and evidence, inspect the
schema/helper/CLI/report/test chain, recompute the deterministic sample
unified-diff artifact digest, test fail-closed scope validation, confirm source
immutability and repeated projection determinism, confirm no approval, policy
evaluation, apply, target mutation, rollback, admission, or trust occurred,
preserve inherited operational-health warning debt, and stop at `needs_review`.

Do not accept the capability, implement apply behavior, create AdapterManifest
or ContextPack v2, run workers, call providers/models/network/Gateway/GitHub,
mutate branches/worktrees, mutate target repositories, publish, or promote.
