# AIDE repository, release and project rollout checkpoint

Date: 2026-09-26. Scope: owner-requested merge/sync/report only. No resumed
implementation, tests, builds, archive generation, target installs or release.

## Repository truth

- Reviewed campaign source/evidence `53be4fc43ab20786de10312d0e5cfaf04e31921a`,
  tree `7b772d73328959afb31197f194b8c12b23ed9abe`, fast-forwarded into dev.
  The final dev closeout identity and observed refs are recorded externally in
  `D:/Projects/AIDE/_recovery/resource-cleanup-20260926/dev-sync-final-observation.json`.
- Main remains `aec53b1d3675f02e2fdd17cc718fdcff6cd4e9f3`: accepted worker
  foundation, not the complete current Lite campaign or a new stable release.
- All local and fetched remote task refs were compared. Only additional unique
  content was historical neutrality evidence at `916d4928`. Its three evidence
  files are preserved byte-identically in this conforming closeout. The two
  original commits/refs and raw formatting failures remain; malformed `fbca5038`
  is not imported into dev ancestry. Its old status is not restored over current truth.
- Local/remote dev and the reused campaign branch are to be aligned at this
  closeout; both physical worktrees remain. Historical branches remain intact.
- Read-only remote tag query returned no tags; GitHub Releases returned `[]`.
  No stable tag, published asset or downloaded consumer proof exists here.
- The filesystem queue had 387 tasks before this bounded closeout item. Historical
  phase and task counts are not a percentage of product completion. Some old
  documents/statuses preserve earlier gates; the adopted profile, exact evidence
  and mandatory coverage matrix determine current acceptance.

## Implemented versus qualified

| Area | Current source and evidence | Remaining release requirement |
| --- | --- | --- |
| Portable Lite delivery | Export/import, deterministic release generator, prior fresh/brownfield archive canaries | Rebuild once from current source; current archives are older previews |
| Customization/update | Project-owned metadata, exact-byte rationale/unknown handling, disabled intent, predecessor and three-way resolutions, explicit feedback | Qualify all declared forms and real version pairs from final bytes |
| Lifecycle | Supported Windows import, owned repair, rollback, removal/detach and effect reconciliation implemented | Complete final integrated interruption/recovery/refusal matrix |
| Partial import recovery | Exact saved plan/input guard independently accepted; seven focused cases recorded passing | Full importer suite and delivered archive qualification still open |
| Resource execution | Explicit roots/volumes, finite reservations, Windows Job limits, sampling, logs/cancel/recovery/retirement; nonwriting inspection | Configure existing approved local pools; qualify actual generator/tool placement |
| Feedback repair | c6d104f6 independently ACCEPT_WITH_NOTES; 3 tests PASS/no skips; real Windows junction; 28 parser forms | Final delivered bytes and broader consumer acceptance |
| Context/evidence/offline | Queue, packets, validators, routing/accounting fixtures and offline foundation | Resolve prior warning classifications and prove final installed/offline behavior |
| Native/hosted automation | Broker, API query and GitHub contract source integrated | Restricted principal, Windows privilege/isolation and hosted effect/race gates |

Recorded tests cover different commits/scopes; they are not summed or presented
as a newly run integrated suite. This closeout checks Git/ref/message/evidence
integrity only. No full test run or stable acceptance is claimed.

## Resource and organization state

34 inactive worktrees were previously retired with refs/unique work preserved;
two remain. Known removed disposable logical bytes total 3,258,721,920, excluding
three unmeasured checkouts. Unknown material, custody and required failures stay.
At 16:31 AEST, free bytes: C 211,532,599,296; D 71,830,855,680;
E 55,903,629,312; physical RAM available 13,447,352,320. Concurrent unrelated
activity prevents attribution of all volume changes to AIDE cleanup.

Both remaining local execution.json configurations were absent. The existing
receipt parent was used only for tiny finite per-run fixtures, not adopted as
an approved permanent bulk pool. No fallback or new machine layout is chosen.
The implemented runner has finite per-job limits and retirement; actual build,
cache and packaging tool locations still require configured, qualified roots.

## Next release: ordered critical path (not started)

1. Recover/configure the owner's existing build/test/temp/cache/output pools,
   finite capacities and volume identities. One intensive job; one Git writer.
2. Run the combined importer/lifecycle suite through the existing bounded runner.
   Close remaining interrupted import/rollback/removal/repair points and any
   actual defects; retain skips and uncertain effects honestly.
3. Batch one current-generator export/release/draft projection. Current ZIP/tar
   previews from generator source 0509e161 do not contain all newer source and
   must not be distributed as the completed candidate.
4. Qualify final bytes in contrasting fresh/brownfield consumers: direct edits,
   metadata and disabled intent, successive updates, three-way conflicts,
   interruption, repair, rollback, detach/removal, context/evidence and offline.
   Qualify the 28 declared candidate CLI forms and support documentation.
5. Freeze source/tree, supported profile, version, dependencies, assets/hashes,
   provenance and qualification manifest. Recheck history: conditional first
   qualified version is 1.0.0 with `aide-lite-v<MAJOR.MINOR.PATCH>`; no version
   is selected now. `aide-lite-pack-v0` names the format, not product SemVer.
6. Obtain independent technical release ACCEPT, then execute delegated main
   promotion, immutable tag and normal publication. Download final assets,
   verify hashes, and prove installation/update from those downloaded bytes.

Candidate first profile is `aide-lite-local-windows`, proposed T3 Limited Support,
with providers disabled/offline after acquisition. It is not currently shipping.
Native/hosted/model-enabled/non-Windows apply promises require their separate
qualification. Service/TUI/Workbench/Commander, broad hosts/providers and wider
adopted programme outcomes remain outside this first profile and unfinished
where required; one Lite release would not complete the whole campaign.

## Deployment and use in our projects (future work only)

- Use AIDE's queue/ExecPlans, compact context, evidence and admitted deterministic
  runner for its own development. Configure roots before further heavy commands.
- After final artifact qualification, prove both new-project and brownfield
  installation in disposable consumers. Prepare actual rollout only for an
  identified target with its own authority and observed state.
- Pilot one admitted project, then expand in small rings. Eureka/Dominium have
  existing adapter/bridge work; that does not prove a current live install or
  authorize blanket mutation. Other projects' files/processes remain protected.
- Every target retains native tools/authority, authored AGENTS/configuration,
  project metadata, queue/memory and disabled choices. Preview update; bind the
  exact plan and predecessor; explain conflicts; apply only the accepted plan.
- Verify target-owned reports, installed health and recovery/removal before
  expanding. Use project-owned adaptations and byte-bound rationale; feedback
  remains explicit local creation with separately authorized sharing.
- Do not roll out current stale previews or claim autonomous native/hosted
  operation from maintainer permissions. No project deployment happened here.

## Stopped state and source-of-truth pointers

The persistent campaign remains blocked/incomplete and is not resumed by this
closeout. No background implementation/test/build job is started. The next
execution needs approved pool information and fresh qualification; no percentage
or finish date is defensible yet.

Canonical detail: parent ExecPlan, mandatory-profile-coverage-2026-09-25.md,
resource-cleanup resource-checkpoint.md, release-versioning.yaml, adopted
scope-and-profiles.md, ROADMAP.md and the relevant task review/evidence records.
