# Admission Versus Conformance

Contract law preserved:

```text
installation != admission
declaration != conformance
conformance != admission
admitted != authorized
```

`AdmissionRecord` binds trust to an exact implementation digest and separate
conformance result refs. Admission records list admitted capability refs
explicitly rather than deriving authority from installation or conformance
alone.
