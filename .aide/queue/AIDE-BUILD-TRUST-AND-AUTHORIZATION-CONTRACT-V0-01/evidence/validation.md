# Validation

Current result: `PASS_WITH_WARNINGS`.

Command-level validation results are recorded in `validation-results.md`.

Summary:

- focused trust contract tests passed;
- direct `trust` CLI status/project/validate commands passed with warnings;
- compile checks passed;
- registered-process, ExecutionHost, and LocalProcess regressions passed;
- task inspect/evidence reports missing evidence zero;
- broad AIDE validation passed;
- diff hygiene passed;
- local-path and secret-like scans passed.
