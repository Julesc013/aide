# AIDE-ACCEPT-LOCAL-PROCESS-EXECUTION-HOST-V0-01

Create and process `AIDE-ACCEPT-LOCAL-PROCESS-EXECUTION-HOST-V0-01`.

Repo truth outranks this prompt. This is an acceptance-only task. Do not repair
implementation.

Accept exactly:

```text
local_process_execution_host_fixture_v0
```

Do not accept:

```text
arbitrary command execution
generic worker harness
autonomous AI worker
remote ExecutionHost
Service/runtime behavior
Workbench behavior
MCP behavior
provider/model/network calls
PreviewSession
DevelopmentTransaction
PatchTransaction apply
public process cancellation API
durable idempotency
scheduler
process-tree management
resource quotas
streaming artifact store
source or target repository mutation
branch/worktree automation
GitHub mutation
release or promotion
```

Verify the build and Repair 02 check chain reports zero remaining material
findings and missing evidence zero. Preserve all failed historical check
evidence.

Stop at `needs_review` with `ACCEPTED_WITH_WARNINGS` and recommend exactly:

```text
AIDE-BUILD-TRUST-AND-AUTHORIZATION-CONTRACT-V0-01
```
