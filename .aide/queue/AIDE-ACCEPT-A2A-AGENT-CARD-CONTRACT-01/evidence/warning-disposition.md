# Warning Disposition

Accepted warnings:

- Full vendored official A2A JSON Schema validation remains deferred.
- A2A remains contract-only, fixture-only, projection-only, non-publishable, non-registered, non-delegating, and non-executable.
- Future `aide://interop` ReferenceID authority remains advisory and does not broaden accepted ReferenceID law.

These warnings do not block acceptance of `minimal_a2a_agent_card_contract` because the accepted scope is limited to deterministic contract and fixture projection.
