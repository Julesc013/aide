# Next Task Prompt

Create `AIDE-LIFECYCLE-FIXTURE-INSTALL-MANAGED-SECTION-APPLY-AUTHORITY-01` as a review-only authority WorkUnit. Decide whether to authorize exactly one fixture-scoped `install-managed-section` managed-section apply execution in `AIDE-LIFECYCLE-FIXTURE-INSTALL-MANAGED-SECTION-APPLY-01`.

If authorized, the authority packet must explicitly name the fixture root, file path, operation, preimage hash, postimage hash, expected report, rollback record destination, allowed paths, forbidden operations preserved, and required checkpoint. If not authorized, reaffirm the block.

Do not execute apply in the authority task.
