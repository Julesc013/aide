# Next Batch

Selected next task: `AIDE-LIFECYCLE-FIXTURE-INSTALL-MANAGED-SECTION-APPLY-AUTHORITY-01`

Goal: Create a reviewed authority packet that explicitly decides whether the first fixture apply execution is authorized.

The authority task should either:

- authorize exactly one fixture apply for `install-managed-section`; or
- reaffirm the block and defer mutation proof.
