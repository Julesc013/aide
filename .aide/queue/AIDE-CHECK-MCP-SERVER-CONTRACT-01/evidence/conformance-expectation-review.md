# Conformance Expectation Review

Conformance expectation count: `22`

IDs are unique and records are marked `declared_not_executed` with
`fixture_or_static_projection_only` evidence status.

The catalogue does not claim live conformance execution.
