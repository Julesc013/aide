# Warning Disposition

Non-blocking warnings:

- full vendored official MCP schema validation remains deferred;
- MCP server behavior remains absent;
- stdio and Streamable HTTP transports remain absent;
- authorization remains absent;
- resource serving, prompt serving, and tool execution remain absent;
- runtime conformance execution remains absent;
- advisory `aide://interop` identity remains warning debt.

These warnings do not downgrade the material findings.
