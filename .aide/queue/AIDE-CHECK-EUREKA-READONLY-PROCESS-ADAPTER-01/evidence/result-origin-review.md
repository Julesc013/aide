# Result Origin Review

The committed invocation result records:

- `result_origin`: `eureka_public_alpha_smoke_json`
- `constructed_success_result`: `false`
- `eureka_json_parsed`: `true`
- `eureka_result.status`: `valid`
- `eureka_result.source_schema`: `public_alpha_smoke.implicit_v0`

The check accepts the source result as a real Eureka-owned command result, with
the warning that it is not the absent requested `public_alpha_readonly_validation.v0`
schema.
