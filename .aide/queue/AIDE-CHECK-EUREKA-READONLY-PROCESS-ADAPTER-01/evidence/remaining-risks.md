# Remaining Risks

- The preferred `scripts/validate_public_alpha_readonly.py` command is absent in the pinned local Eureka checkout.
- The selected command uses `public_alpha_smoke.implicit_v0`, not `public_alpha_readonly_validation.v0`.
- Mutation evidence is limited to declared Git probe coverage.
- RegisteredProcessExecutionProvider v0 remains proposed until the separate acceptance gate.

No material findings remain for this check.
