# Validation

The independent check result is `PASS_WITH_WARNINGS`.

Material finding count: `0`.

Missing evidence: `0`.

Warnings remain for:

- selected command differs from the preferred absent command;
- selected JSON shape is `public_alpha_smoke.implicit_v0`;
- mutation proof is limited to declared Git probe coverage;
- provider remains proposed and unaccepted.
