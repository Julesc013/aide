# Result Axis Review

The shared outcome model remains separated:

- `transport_outcome`: `transport_started`
- `process_outcome`: `exit_zero`
- `decoder_outcome`: `decoded`
- `domain_outcome`: `typed_result`
- `reason_code`: empty

Provider acceptance remains a separate queue gate.
