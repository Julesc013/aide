# Exact activation preparation

Status: **not ready for activation approval**. Concrete known settings and remaining prerequisites follow. This is not standing delegation.

| Input | Proposed setting | Evidence or missing prerequisite |
| --- | --- | --- |
| Runtime | AIDE local 7d8bf19d878fd9ad29859a6cba4b7de64ad80ecc plus this directory's source manifest; Codex CLI 0.145.0 | Local uncommitted prototype, not an accepted runtime revision. |
| Models | coding gpt-5.6-terra; assurance gpt-5.6-luna | Explicit proposed routing; account availability still needs verification. |
| Host | BLACKGLASS-WIN1, Windows owned-process adapter | Process containment tested; actual Codex descendants, filesystem/credential separation need host qualification. |
| Scope | At most two real admitted tasks, one writer and fresh assurance per task | Exact tasks/base preparation remain unselected. Do not substitute fixtures or copy the entire product backlog. |
| Programme limit | 60 minutes; 15 minutes per process; four worker sessions; two total task attempts; zero blind coding retries | Proposed limits, not permission to spend. |
| Other resources | Eight owned processes per Job; 4 GiB Job memory; 8 MiB captured output; 1 GiB state threshold; 5 GiB free-space floor; 60 integration observations | Monitored disk thresholds require a separate disposable-volume quota. |
| Spend/auth | Existing Codex allowance; ChatGPT login only; no API credentials, purchases or billing changes | Allowance/paid usage posture and credential isolation unverified. |
| State | C:\Users\Jules\AppData\Local\AIDE\Continuous\facman-pilot\state | Proposed protected root; not created or ACL-qualified. |
| Clones | Independent clones outside runtime/state, through each repository's approved hygiene/branch plan | No clones or secondary worktrees created. |
| Integration | Protected broker enforcing current dev policy and exact independent evidence; no direct protected push, self-approval, force push, signing or publication | Broker implementation/provisioning and owner-reviewed delegation required. Current FacMan merge declarations are false. |
| Control | Persistent status, pause, drain, cancellation, emergency stop and resume | Implemented. OS restart supervision/notification transport not installed; no external messages sent. |
| Endpoint | Complete FacMan 0.1 machine/package candidate including WinForms and GTK | Human experience/signing/publication gates retained. Full endpoint not claimed by the pilot. |

## Before the one activation decision

1. Resolve AIDE runtime qualification/branch plan; pin the resulting accepted revision and manifest.
2. Implement and qualify the protected integration broker against actual gates. Prepare owner-reviewed standing delegation; operational status cannot rewrite admission or authority.
3. Qualify worker/broker credential/filesystem boundaries, volume quota and supervisor restart on the named host.
4. Bind two useful task IDs to authoritative admissions. Sequential tasks in one repository need dependency-derived base preparation and fresh evidence.
5. Set actual expiry, account usage authorization, protected evidence references, exact roots and executable pins. Then request approval for one exact configuration digest.
6. Run the real two-task demonstration and failure qualification before recording live acceptance.

The current gate is .aide/policies/review-gates.yaml: “Wait for explicit human review before continuing.” It applies to authority changes and integration beyond local commits. The user's explicit request authorizes bounded implementation and queue admission; it does not supply these future activation inputs. No skill introduced an additional approval gate.

This incomplete preparation is not a request to approve placeholder values.

