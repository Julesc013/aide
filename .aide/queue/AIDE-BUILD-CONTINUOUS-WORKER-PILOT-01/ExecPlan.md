# Continuous worker pilot

Status: local prototype implemented and validated; live acceptance incomplete.

## Objective and boundary

Build a separate coordinator using Codex CLI 0.145.0 noninteractive JSON/schema interface, durable claims/intents/evidence, one writer, owned-process cancellation, explicit operator controls and exact-source independent assurance. Keep the accepted fixture host and FacMan advisory controller unchanged. The endpoint for eventual product execution remains complete 0.1 including WinForms and GTK, with human experience/signing/publication distinct.

## Baseline and authorization

Local AIDE HEAD: 7d8bf19d878fd9ad29859a6cba4b7de64ad80ecc. Local baseline is clean before intake reports. The remote c39f47ea observation belongs to the pasted assessment and is not a fetched baseline. The user explicitly requested admission and implementation; that authorizes this queue addition and bounded local implementation. The intake heuristic classified the combined prompt as blocked release/Git work because it contains those topics; split implementation from activation here rather than treating that classifier as authority to execute. Existing review policies are unchanged.

## Sequence

1. Inspect/noop/recover and record intake/preflight; add this queue item.
2. Add durable state with transactional claims and intent-before-effects; retain uncertain attempts without redispatch.
3. Add separate Windows owned-process host, bounded output and resource checks; require quiescence before reuse.
4. Add Codex exec JSON/schema adapter and explicit session identities; fresh assurance context; consume existing task source references.
5. Add coordinator pipeline, operator controls, policy/source drift checks and integration receipts; fail closed at absent integration authority.
6. Run recovery/fault/real-child-process tests; independent code assurance and repair.
7. Prepare exact activation configuration and report honestly which live two-task and host/integration prerequisites remain.

## Validation and exit

Meaningful deterministic unit tests plus real process and supervisor crash tests, bounded disk/output/timeout, duplicate claims, source drift, uncertainty, controls, failing tests, independent assurance binding and integration reconciliation. Separate synthetic qualification from the required two real admitted tasks in distinct worker sessions through legitimate integration. Do not mark live acceptance passed until durable receipts prove those events.

## Allowed paths

As listed in task.yaml. Helper-generated intake/preflight reports are retained as diagnostic outputs and are not silently promoted into new product authority. No branch/worktree/push/merge mutation is currently planned. Code remains a reviewable local change until the helper plan and exact integration permission allow more.

## Recorded outcome

Eight runtime modules, three test files and 51 passing local tests. Independent assurance findings repaired. See evidence/validation.md and evidence/activation-proposal.md for the exact unqualified live boundaries. No model dispatch, policy activation, integration, branch creation or product acceptance occurred.
