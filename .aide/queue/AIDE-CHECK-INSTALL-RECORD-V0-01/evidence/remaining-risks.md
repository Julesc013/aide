# Remaining Risks

- InstallRecord v0 is still proposed until acceptance completes.
- This check verifies metadata/projection/validation behavior only; it does not authorize install apply, target scans, target mutation, release publication, or provider/model/network calls.
- Future target repositories must produce their own target-local install records; source-generated reports remain source evidence only.
