# Warning Disposition

Accepted warnings:

- Reduced independence: this check ran in the same Codex thread as the source build.
- ExecutionHost contract v0 remains projection-only.
- Full external Draft 2020-12 validation was not separately installed or required.
- Python launcher selection differs when nested under Python on this host; the harness used the active interpreter path and scrubbed it from evidence.

These warnings do not change the result because no material contract,
determinism, portability, authority-boundary, evidence-truthfulness, or
interoperability defect remains.
