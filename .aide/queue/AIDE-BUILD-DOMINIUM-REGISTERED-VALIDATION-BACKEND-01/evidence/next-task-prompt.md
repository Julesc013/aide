# Next Task Prompt

```text
Create and process
AIDE-CHECK-DOMINIUM-REGISTERED-VALIDATION-BACKEND-01.

Independently check AIDE-BUILD-DOMINIUM-REGISTERED-VALIDATION-BACKEND-01.

Verify:
- the real Dominium CLI was entered exactly once;
- the fixture callable was not used as the executor;
- the result originated from Dominium stdout JSON;
- the service adapter boundary was reached or a typed Dominium refusal proves the path;
- unsupported and invalid requests spawn no process;
- Dominium checkout state remained clean and unchanged;
- reports are scrubbed of local absolute paths and secrets;
- EvidencePacket, EventRecord, WorkUnit, ContextDescriptor, ContextPack, and projection records are deterministic and valid;
- missing_evidence is 0.

If the build passes, recommend exactly:
AIDE-ACCEPT-DOMINIUM-REGISTERED-VALIDATION-BACKEND-01

If a material defect remains, recommend a bounded repair task.
```
