# Blocked Baseline

ContextPack v2 build is blocked because:

- AdapterManifest acceptance is `BLOCKED`.
- PatchTransaction acceptance is `BLOCKED`.
- The live queue continues to route to
  `AIDE-BUILD-PATCH-TRANSACTION-SCHEMA-REPAIR-01`.

No ContextPack v2 capability is built by this task.
