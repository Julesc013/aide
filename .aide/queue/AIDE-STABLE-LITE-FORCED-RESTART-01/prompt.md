# Bounded qualification prompt

From current accepted Lite `dev`, run disposable Windows consumers using the
exact generated ZIP. Force child-process death immediately after chosen durable
lifecycle effects, start an unmodified CLI process, and prove precise safe
reconciliation. Keep generated bytes read-only and preserve command/effect
receipts. Route real defects to a separately reviewed source repair.
