# Remaining risks and next executable action

An abrupt first-payload import exit is classified `partial`. The fresh CLI
refuses automatic replay and keeps exact intent/project bytes. This is a safe
refusal, not completed recovery. Route an exact bounded source WorkUnit for
partial-state reconciliation with target, pack, intent, owner-byte and
competing-writer checks. Review the changed source independently and rerun
delivered-byte restart qualification before the stable Lite release decision.

The completed-effect canaries use one Windows/Python environment and a local
preview ZIP. The rollback successor is synthetic. OS network isolation,
published predecessor pairs, hostile process races, final assets and downloaded
consumer evidence remain separate release gates. No main/tag/publication
authorization is inferred from this evidence review.
