# Clause Dispositions

| Source aliases | Disposition | Live requirements | Semantic boundary |
|---|---|---|---|
| `UR-WORK-01` through `UR-WORK-07` | adapt and adopt | `AIDE-WORK-001` through `AIDE-WORK-007` | Preserve the live filesystem queue; no schema migration or dispatch authority. |
| `UR-CAP-01` through `UR-CAP-07` | adapt, split effects, and adopt | `AIDE-CAP-001` through `AIDE-CAP-007` | Add billing, integration, and publication distinctions already required by live authority policy; no capability activation. |
| `UR-ID-01` through `UR-ID-07` | adapt and adopt | `AIDE-ID-001` through `AIDE-ID-007` | Candidate IDs become source aliases; no existing identifier or digest migration. |
| Generated front matter | reject as live metadata | none | Generated package status is not repository adoption status. |
| Package-local requirement and case registers | defer | external S21 custody | Bulk records require their own semantic reconciliation. |
| Candidate acceptance cases | retain as acceptance direction | prose in each live contract | Desired tests only; none are recorded as run. |

The live text separates desired behavior from implementation, qualification,
activation, support, and effect permission. Source aliases remain attributable
through the verified S21 custody chain.
