# Prompt

Accept the registered Dominium validation backend only as:

```text
dominium_registered_validation_command_boundary_invocation_v0
```

The accepted meaning is limited to one narrowly registered Dominium validation
command invoked through one bounded local process boundary with preflight,
exact argv, `shell=False`, constrained environment, timeout, typed
result/refusal capture, declared state-probe comparison, evidence, and event
projection.

Preserve these warnings:

- the observed domain outcome was a typed refusal;
- aggregate validation did not succeed;
- aggregate validation execution is not accepted;
- service-adapter entry is not accepted without independent Dominium evidence;
- no generic Dominium command runner or generic registered process provider is
  accepted.

Recommend only:

```text
AIDE-BUILD-REGISTERED-PROCESS-EXECUTION-PROVIDER-V0-01
```
