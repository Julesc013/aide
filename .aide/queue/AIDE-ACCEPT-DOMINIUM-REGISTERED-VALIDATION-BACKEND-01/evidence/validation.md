# Validation

Acceptance validation checks that:

- the relabel check passed with zero material findings and zero missing evidence;
- the accepted capability label is exactly
  `dominium_registered_validation_command_boundary_invocation_v0`;
- aggregate-validation execution and success are not accepted;
- service-adapter entry is not accepted;
- explicit non-capabilities are preserved;
- the next task is exactly
  `AIDE-BUILD-REGISTERED-PROCESS-EXECUTION-PROVIDER-V0-01`.
