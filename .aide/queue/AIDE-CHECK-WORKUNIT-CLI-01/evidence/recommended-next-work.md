# Recommended Next Work

Result: PASS_WITH_WARNINGS

Recommended next task: `AIDE-ACCEPT-WORKUNIT-CLI-01`.

Do not start `AIDE-BUILD-WORKUNIT-CLI-MUTATION-01` until the read-only WorkUnit CLI has an acceptance result.

Seed:

```text
AIDE-ACCEPT-WORKUNIT-CLI-01
```
