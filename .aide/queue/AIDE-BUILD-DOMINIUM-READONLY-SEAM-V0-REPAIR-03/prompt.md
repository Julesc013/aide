# AIDE-BUILD-DOMINIUM-READONLY-SEAM-V0-REPAIR-03

Create and process `AIDE-BUILD-DOMINIUM-READONLY-SEAM-V0-REPAIR-03`.

Use `.aide/queue/index.yaml` as canonical queue truth. Live repository state and task-local evidence outrank this prompt, generated packets, previous chat, and stale planning notes.

Repair exactly the 15 material findings recorded by `AIDE-CHECK-DOMINIUM-READONLY-SEAM-V0-REPAIR-02`:

1. `schema.kind_specific_spec_types`
2. `schema.false_boundary_required`
3. `schema.unbounded_authority_fields`
4. `fixtures.production_remove_missing`
5. `fixtures.production_replace_missing`
6. `fixtures.production_index_strictness`
7. `conformance.unsupported_operation_semantics`
8. `conformance.no_write_semantics`
9. `conformance.network_semantics`
10. `operation.raw_trace_auditability`
11. `operation.git_network_classification`
12. `operation.injection_evidence`
13. `portability.manifest_driven_copy`
14. `portability.sanitized_environment`
15. `cli.unsupported_extended_verbs`

Do not split these findings into microtasks. Do not redesign the seam, widen its capability scope, implement the later WorkUnit validation slice, accept the seam, or run the independent Repair 03 check. Stop at `needs_review` and recommend exactly `AIDE-CHECK-DOMINIUM-READONLY-SEAM-V0-REPAIR-03`.

Preserve all historical evidence under the prior build, check, Repair 01, Repair 02, and Repair 02 check queue/report roots. Record immutable before/after hashes for those roots.

Allowed paths and explicit non-goals are defined in `task.yaml` and `ExecPlan.md`.
