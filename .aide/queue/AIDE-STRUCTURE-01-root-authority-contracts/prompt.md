# Prompt: AIDE-STRUCTURE-01-root-authority-contracts

Proceed with Track B after the current-truth and root-authority audit.

Use live repo truth, not transcript assumptions. The dependency task is
`AIDE-STRUCTURE-00-current-truth-and-root-authority-audit`, which stopped at
`needs_review` with `PASS_WITH_WARNINGS` and recommended this task as the next
safe step.

Create bounded root authority contracts and planning artifacts:

- a machine-readable root authority policy;
- a human-readable governance note;
- a repository layout reference;
- a root-authority contract report;
- follow-up Track B prompts for status sync, fate maps, dot-tool interop, and
  root-plan work;
- queue evidence and validation records.

Do not move files, delete files, rewrite references, create aliases or shims,
create new top-level roots, promote generated outputs to source truth, accept
previous queue work, mutate branches, mutate target repos, call providers or
models, use network services, publish releases, or implement Track A protocol
features.

Stop at `needs_review`.
