# Remaining Risks

## Warnings

- The root authority contract is review-gated and should not be treated as
  accepted beyond this `needs_review` task state.
- README, OKF pages, generated context, and some historical task metadata still
  contain stale status or next-task wording.
- `shared`, `platforms`, `research`, and `specs` still need a no-apply fate map.
- `.agents` and `.codex` still need explicit interop/projection policy before
  expansion.
- `tools`, `tests`, `examples`, and `archive` remain add-only candidates; this
  task does not justify creating them.
- Generated reports remain evidence or projections, not canonical truth.

## Recommended Next Task

`AIDE-STRUCTURE-02-status-doc-sync`
