# Report Consistency Evidence

The acceptance reports agree that:

- result is `BLOCKED`;
- PatchTransaction is not accepted;
- `AIDE-CHECK-PATCH-TRANSACTION-SCHEMA-01` is the blocking task;
- the blocking result is `FAILED_VALIDATION`;
- the only next task is `AIDE-BUILD-PATCH-TRANSACTION-SCHEMA-REPAIR-01`.

JSON parsing is recorded in validation evidence.
