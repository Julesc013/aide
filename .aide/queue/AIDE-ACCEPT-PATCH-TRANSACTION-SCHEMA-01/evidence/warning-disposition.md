# Warning Disposition Evidence

The acceptance blocker is material, not warning-class debt.

Blocking:

- `path_scope_drive_prefixed_relative_accepted`
- `path_scope_duplicate_normalization_accepted`

Non-blocking deferred capabilities remain absent:

- general diff parser;
- artifact resolver;
- VCS reachability verification;
- policy engine;
- approval engine;
- apply engine;
- rollback execution;
- conformance runner;
- admission and trust;
- runtime.
