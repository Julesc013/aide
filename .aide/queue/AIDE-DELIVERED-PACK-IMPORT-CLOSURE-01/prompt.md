# Prompt: Delivered Pack Import Closure

Continue the admitted AIDE convergence campaign with the first independent
product slice from the stable implementation mission. Characterize and reuse
the existing exporter, release builder, importer, and lifecycle machinery.
Make extracted release bytes independently importable into disposable fresh
repositories, starting from a failing consumer regression. Preserve exact
forbidden-path, branch, main-promotion, tag, publication, and target-mutation
gates. This is implementation and local qualification, not release approval.
