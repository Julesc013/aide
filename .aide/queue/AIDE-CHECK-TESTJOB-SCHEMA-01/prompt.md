# AIDE-CHECK-TESTJOB-SCHEMA-01

Check, do not build.

Independently review `AIDE-BUILD-TESTJOB-SCHEMA-01` and reported commit `017b7e639cd2fc48a5bfad8bf91e5665f0d56e9e`. Verify the minimal TestJob helper/schema/projection/validation slice is coherent, additive, source traceable, compatible with accepted predecessor layers, and truthful about not implementing Test Broker runtime, async test execution, test job submission/run/retry/summarize runtime, worker execution, WorkUnit claim/run/finish/repair, leases, scheduler, supervisor, Service, Commander, provider adapters, branch/worktree automation, target apply, active apply, rollback, release, promotion, network, Gateway, GitHub, or model/provider calls.

Use live `.aide/queue/` truth over stale latest-task packets. The user-supplied frozen plan for this turn makes the next task `AIDE-ACCEPT-TESTJOB-SCHEMA-01`; after acceptance, continue to `AIDE-BUILD-REFERENCE-ID-SCHEME-01`, not PatchTransaction, unless a later reviewed queue item changes that order.

End with `PASS`, `PASS_WITH_WARNINGS`, `FAILED_VALIDATION`, `BLOCKED`, or `PARTIAL` and concrete next-task guidance.
