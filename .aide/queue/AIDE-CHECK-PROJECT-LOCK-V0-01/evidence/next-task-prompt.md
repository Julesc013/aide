# Next Task Prompt

```text
Create and process:

AIDE-ACCEPT-PROJECT-LOCK-V0-01

Accept exactly:

project_lock_v0

Do not accept install truth, install/update/apply behavior, admission,
authorization, target repository mutation, release publication, network/provider
calls, Workbench/MCP runtime, OwnershipLedger v1, InstallRecord v0, or any
downstream distribution object.

Verify the ProjectLock build and independent check both report zero material
findings and zero missing evidence. Stop at needs_review and recommend exactly:

AIDE-BUILD-OWNERSHIP-LEDGER-V1-01
```
