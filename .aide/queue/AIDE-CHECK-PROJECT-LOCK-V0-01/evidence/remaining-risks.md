# Remaining Risks

- ProjectLock v0 remains proposed until the acceptance task completes.
- The accepted scope is a lock/selection object only; it does not implement
  install truth, apply behavior, admission, authorization, target mutation,
  release publication, runtime, or OwnershipLedger behavior.
- DistributionManifest v1 remains accepted with warnings from its own
  acceptance boundary.

No material ProjectLock v0 defects were found by this check.
