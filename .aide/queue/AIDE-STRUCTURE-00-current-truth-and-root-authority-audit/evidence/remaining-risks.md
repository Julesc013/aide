# Remaining Risks

- Root authority contracts are not yet written.
- README, OKF, generated context, and some historical task metadata still have
  stale status or next-task wording.
- `shared`, `platforms`, `research`, and `specs` have no reviewed fate map yet.
- `.agents` and `.codex` still need explicit interop/projection policy.
- `tools`, `tests`, `examples`, and `archive` are only candidate future roots;
  this task did not justify or create them.
- Root recycling reports mark 5,047 files review-required, so future movement
  must remain map-first and review-gated.
- Refactor maps currently contain 0 move entries and therefore cannot support
  any actual structural move.
- The generated latest task packet points at this audit, but Reconciler still
  treats it as stale relative to accepted OKF routing.
- The task-owned dirty tree must be classified and committed before Git helper
  planning is unblocked.
