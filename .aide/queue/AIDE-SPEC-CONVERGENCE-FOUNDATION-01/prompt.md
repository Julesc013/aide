# Prompt

Keep the verified unified specification archive external. Reconcile its clauses
into the live `specs/` tree in reviewable families, improve navigation and
layout, preserve existing architecture and boot-slice records, and distinguish
adopted design from implemented, qualified, activated, and supported behavior.
