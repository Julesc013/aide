# Remaining Risks

- AdapterManifest is not accepted until the resume acceptance task runs.
- No admission or trust engine exists.
- No adapter execution, worker launch, sandbox creation, credential resolution,
  provider/model/Gateway/network call, GitHub mutation, patch apply, target
  repository mutation, runtime, Service, Commander, Workbench, Test Broker, or
  ContextPack v2 behavior exists.
- Full JSON Schema Draft validation remains future work.
