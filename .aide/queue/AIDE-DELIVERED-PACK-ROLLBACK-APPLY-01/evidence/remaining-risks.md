# Remaining risks and boundaries

- Rollback needs the exact validated predecessor pack and equal safe payload target sets. Changed path sets and unavailable predecessor bytes are outside this first slice and remain parent-campaign gaps.
- A later project edit after update must refuse restore. Mixed interrupted effects require explicit recovery rather than a fresh rollback.
- Only the Windows effect path is implemented. No real target, `dev`, generated release artifact, main ref, tag, or publication was changed by this task branch.
