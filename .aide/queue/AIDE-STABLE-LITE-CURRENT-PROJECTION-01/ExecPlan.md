# ExecPlan: AIDE-STABLE-LITE-CURRENT-PROJECTION-01

## Objective, scope and dependencies

Project accepted Task OS and Q48 source from clean `59286688`, tree
`1a3828b6`, which descends from `dev@d292253b`. The accepted source subjects
are `f00d937e`, `52e1f194` and `bc1867a0`; their exact reviews remain in
their own WorkUnits. This task owns only generated outputs, bounded evidence
and one qualified `dev` effect. Source code is read-only here.

## Plan

1. Admit and commit this WorkUnit, leaving a clean source checkout. Confirm
   source ancestry, exact review hashes and current refs. Record any drift.
2. Run `export-pack`, `changelog preview`, `release bundle`, `release validate`,
   `release draft`, `release draft-validate` serially from the current clean
   generator. Preserve command logs, exits and SHA-256 values externally.
3. Verify exact archive members, checksums, source binding, no-publish draft,
   canonical `pack-status`, `validate`, `doctor`, Q47/Q48 and Task OS suites.
   In extracted ZIP and tar consumers, prove empty queue neutrality and a
   nonempty project-owned queue, with source-only work excluded.
4. Commit exact generated outputs. If the first post-commit replay changes
   HEAD-bound metadata, retain the failed zero-change result and commit the
   bounded metadata convergence. Four-command release replay from clean HEAD
   must then change zero tracked/untracked files and preserve archive hashes.
5. Obtain independent exact artifact/effect verdict. Freshly verify Windows
   identity, shared worktree writers, clean candidate and `dev`, current refs,
   ancestry, review identity and archive digests. Fast-forward and normally
   push `dev` only with qualified GO; observe local/tracking/remote/API refs.
6. Update evidence and parent coverage. Keep final version, supported profile,
   main, tag, publication and downloaded-byte proof as later gates.

## Recovery

If generation or review fails, preserve the failing command/output and repair
the appropriate source in a separate bounded task. An uncertain Git effect
requires read-back before replay. Never call source-only tests artifact
qualification or reuse an approval tied to changed bytes.

## Progress

- [x] Confirm three accepted source subjects and admit this bounded task.
- [ ] Generate and qualify exact preview bytes.
- [ ] Prove committed zero-change replay and installed target queue behavior.
- [ ] Obtain independent artifact/effect review and qualified dev effect.
