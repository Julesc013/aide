# Source Chain Review

The accepted proof chain covers:

- Dominium: external complex product/engine command boundary, typed refusal preserved.
- AIDE: same-repository deterministic validation success.
- Eureka: unrelated external evidence/resolution domain, typed success with warnings.

All three reuse `registered_process_execution_provider_v0` through the same
invocation, binding, receipt, and outcome model.
