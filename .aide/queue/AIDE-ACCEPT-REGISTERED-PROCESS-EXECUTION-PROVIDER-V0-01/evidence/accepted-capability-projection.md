# Accepted Capability Projection

Accepted capability:

```text
registered_process_execution_provider_v0
```

Accepted scope:

```text
deterministic_registered_shell_free_process_capability_execution
```

Accepted status:

```text
ACCEPTED_WITH_WARNINGS
```

The provider is accepted as a deterministic capability execution substrate, not
as worker execution or a host runtime.
