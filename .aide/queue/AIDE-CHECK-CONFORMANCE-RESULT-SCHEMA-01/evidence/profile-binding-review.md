# Profile Binding Review

Status:

```text
PASS
```

The result binds to:

```text
aide://conformance-profile/minimal_capability_manifest-v1.0.0
```

The profile id is `minimal_capability_manifest`, version is `1.0.0`, and the
profile lifecycle snapshot is `candidate`.

Profile ref/version binding is correct. Profile digest binding is reviewed
separately and fails validation.
