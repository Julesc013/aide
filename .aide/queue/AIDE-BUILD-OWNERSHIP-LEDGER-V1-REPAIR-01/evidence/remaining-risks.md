# Remaining Risks

- OwnershipLedger v1 is still proposed until independent repair check and acceptance.
- Q43 migration is projection-only and does not prove install-state migration apply.
- InstallRecord, MigrationRecord, UpdatePlan, RollbackBundle, and apply behavior remain unimplemented.
- The fixture corpus is deterministic but still requires independent adversarial check before downstream dependency.
