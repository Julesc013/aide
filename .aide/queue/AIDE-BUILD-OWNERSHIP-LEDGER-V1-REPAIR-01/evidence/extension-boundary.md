# Extension Boundary

The repair preserves optional extension maps and continues to fail closed on
`requires.*` extension keys. Unknown optional features remain tolerated as
warnings and are not accepted as authority or apply behavior.
