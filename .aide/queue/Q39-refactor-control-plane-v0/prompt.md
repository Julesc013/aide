# Q39 Prompt Summary

Implement Refactor Control Plane v0.

Q39 consumes the Q36 intent compiler posture, Q37 repo intelligence outputs,
and Q38 file quality evidence to create deterministic, repo-local, dry-run
refactor planning infrastructure. It must define policies, schemas, commands,
example readiness artifacts, tests, golden tasks, docs, evidence, and export
support for future structural changes.

The phase is planning-only. It must not move files, delete files, rewrite
references, apply migrations, mutate branches, mutate target repositories,
call providers/models, use network calls, or auto-fix source/docs/tests. It
prepares Q40 Root Recycling Framework v0.
