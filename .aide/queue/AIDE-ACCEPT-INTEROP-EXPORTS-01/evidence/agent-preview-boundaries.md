# Agent Preview Boundaries

Reviewed:

- `AGENTS.md.preview`
- `CLAUDE.md.preview`
- `copilot-instructions.md.preview`
- `aider.conf.yml.preview`

All remain preview-only. None is installed into a live tool path. None grants
provider/model execution, network access, shell authority, branch/worktree
automation, GitHub mutation, PatchTransaction apply, runtime behavior, or target
repository mutation.
