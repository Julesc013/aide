# Format Validation

Passed:

- JSON parsing for manifest, MCP preview, A2A preview, build export index,
  build projection report, check report, and acceptance report.
- Bounded structural review for `aider.conf.yml.preview`.
- Markdown preview UTF-8 readability checks.

No external dependency was added.
