# Projection Authority Review

Accepted model:

```text
accepted AIDE inputs
-> deterministic export projection
-> preview artifact
```

Rejected model:

```text
preview manually edited
-> becomes AIDE authority
```

The manifest is an inventory and integrity record only.
