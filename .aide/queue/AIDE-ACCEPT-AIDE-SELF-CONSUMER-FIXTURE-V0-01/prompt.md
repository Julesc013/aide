# Prompt: AIDE-ACCEPT-AIDE-SELF-CONSUMER-FIXTURE-V0-01

Create and process `AIDE-ACCEPT-AIDE-SELF-CONSUMER-FIXTURE-V0-01`.

Repo truth outranks this prompt.

Accept only `aide_self_consumer_fixture_v0` after the build and check chain remains complete, evidence remains intact, material finding count is `0`, and missing evidence is `0`.

Acceptance includes:

- fixture corpus;
- self-consumer fresh install, profile generation, upgrade, same-version idempotence, rollback, uninstall/preserve, and offline proof;
- source-repo-vs-installed-target distinction;
- target-owned state preservation;
- DistributionApplyEngine-backed UpdatePlan, RollbackBundle, accepted-context, UpdateReceipt, refusal, and canonical fixture preservation boundaries.

Acceptance excludes:

- real target apply;
- AIDE source repo self-apply;
- canary readiness;
- public release readiness;
- release artifact generation or publication;
- GitHub Release source;
- network/provider/model calls;
- branch/worktree automation;
- push;
- ScreenSave, Eureka, Dominium, Carbon, or external repository mutation.

If accepted and stale DistributionApplyEngine routing text remains visible, recommend exactly `AIDE-BUILD-DISTRIBUTION-APPLY-ROUTING-TEXT-REPAIR-01`.

Stop at `needs_review`.
