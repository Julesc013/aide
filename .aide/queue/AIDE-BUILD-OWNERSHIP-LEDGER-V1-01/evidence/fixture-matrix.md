# Fixture Matrix

The fixture corpus contains valid cases for minimal ledger, extension
round-trip, and reordered records.

Invalid fixtures cover every refusal class in the build matrix, including
ProjectLock digest mismatch, taxonomy failures, duplicate records, apply
enablement, unsafe paths, source latest contamination, unknown required feature,
and required extension rejection.
