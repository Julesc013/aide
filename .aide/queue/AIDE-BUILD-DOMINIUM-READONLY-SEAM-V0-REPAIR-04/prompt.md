# AIDE-BUILD-DOMINIUM-READONLY-SEAM-V0-REPAIR-04

Create and process `AIDE-BUILD-DOMINIUM-READONLY-SEAM-V0-REPAIR-04`.

Use `.aide/queue/index.yaml` as canonical queue truth. Live repository state and task-local evidence outrank this prompt, generated packets, previous chat, and stale planning notes.

Repair exactly the 12 material findings recorded by `AIDE-CHECK-DOMINIUM-READONLY-SEAM-V0-REPAIR-03`:

1. `schema.kind_specific_defs_present`
2. `schema.authority_extensions_bounded`
3. `fixture.production_requires_value`
4. `fixture.production_rejects_unicode_decimal_indexes`
5. `fixture.production_forbidden_key_set_complete`
6. `conformance.unsupported_uses_actual_cli_dispatch`
7. `conformance.no_write_surrounds_actual_operations`
8. `conformance.guard_evidence_is_exercised`
9. `operation.aggregate_key_preserves_semantics`
10. `operation.guard_report_is_not_static`
11. `portability.required_child_output_set_complete`
12. `cli.unsupported_verbs_typed_refusal`

Do not split these findings into microtasks. Do not redesign the seam, widen its capability scope, implement the later WorkUnit validation slice, accept the seam, or run the independent Repair 04 check. Stop at `needs_review` and recommend exactly `AIDE-CHECK-DOMINIUM-READONLY-SEAM-V0-REPAIR-04`.

Preserve all historical evidence under the prior build, check, Repair 01, Repair 02, Repair 03, and check-report queue/report roots. Record immutable before/after hashes for those roots.

Allowed paths and explicit non-goals are defined in `task.yaml` and `ExecPlan.md`.
