# Local State Boundary

- default runtime state path: `.aide.local/service`
- fixture commands use temporary directories by default
- no `.aide.local/service` database or payload was created or committed
- pre-existing `.aide.local/profiles` files are unrelated local ignored state
- `.aide.local/` remains protected by repository ignore policy
