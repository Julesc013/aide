# XCHECK-01R Normalized Prompt

The operator requested immediate execution of the queued audit prompt:

`XCHECK-01R-cross-repo-aide-adoption-validation-efficiency-taskos-readiness-audit`

The task is an audit, verification, reconciliation, and next-wave planning
checkpoint. It is not feature implementation, install apply, target mutation,
branch mutation, merge, push, promotion, release publication, or Task OS apply
automation.

Required outputs include a queue packet, evidence files, compact top-level
reports, validation evidence, a selected next prompt packet, and a structured
commit. The audit must answer current AIDE source state, export/release state,
Dominium and Eureka target-local adoption state if available, pack/adoption
drift, preservation, validation efficiency, test telemetry readiness, Task OS
readiness, capability reality, warning disposition, and the next X-series plan
from repository evidence rather than chat memory.

Primary correction from the prompt:

Validation tiering and async test telemetry must come before automatic task
orchestration, branch dispatch, repair apply, checkpoint promotion, or more
Eureka product expansion.

The raw pasted prompt is intentionally not copied in full here. This file is the
bounded queue prompt record for the WorkUnit.
