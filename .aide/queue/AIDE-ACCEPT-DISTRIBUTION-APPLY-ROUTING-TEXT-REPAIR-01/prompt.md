# Prompt

Create and process `AIDE-ACCEPT-DISTRIBUTION-APPLY-ROUTING-TEXT-REPAIR-01`.

Repo truth outranks this prompt. Accept only the verified operator-facing DistributionApply routing/status text repair as `distribution_apply_routing_text_repair_v0`.

Do not modify implementation, tests, fixtures, DistributionApplyEngine behavior, accepted protocol schemas, accepted capability semantics, target repositories, `.aide.local/**`, release/package artifacts, provider/model/network behavior, branches/worktrees, or external repositories. Stop at `needs_review`.
