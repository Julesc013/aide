# Accepted Boundary

Accepted boundary label: `distribution_apply_routing_text_repair_v0`.

This is not a product/runtime capability and does not add distribution apply authority.

The accepted boundary is limited to:

- operator-facing `distribution-apply status` routing text;
- operator-facing `distribution-apply plan` routing text;
- operator-facing `distribution-apply verify` routing text;
- visibility of accepted fixture capability `aide_self_consumer_fixture_v0`;
- bare non-mutating default plan output;
- no-apply/no-release/no-canary/no-network/no-branch boundary text preservation.

The accepted boundary does not include implementation behavior, test behavior, fixture state, DistributionApplyEngine semantics, protocol schemas, or accepted capability semantics.
