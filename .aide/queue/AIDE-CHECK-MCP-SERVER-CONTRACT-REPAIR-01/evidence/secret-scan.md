# Secret Scan

Secret-like scan over changed check files is part of final validation.

```text
secret_scan_material PASS findings=0 files=3
```

The check reports contain protocol terms such as authorization and credentials
as non-capability language, not credential values.
