# Fixture Report Consistency

The fixture index matches current fixture bytes:

```text
independent_mcp_fixture_check PASS fixtures=15 indexed=15
```

Counts remain:

- resources: `10`
- tools: `7`
- prompts: `0`
- fixtures: `15`

Each fixture index SHA-256 value was recomputed independently and matched.
