# No Forbidden Operations

This check did not:

- repair implementation;
- modify MCP schema/helper/tests/fixtures;
- alter build, failed-check, or repair reports;
- accept the MCP contract;
- download MCP SDKs or schemas;
- install dependencies;
- start a server or launch a subprocess;
- bind sockets or send HTTP;
- implement SSE, sessions, authorization, or credential resolution;
- serve resources or prompts;
- execute tools;
- dispatch workers;
- call providers or models;
- apply PatchTransactions;
- mutate target repositories;
- implement A2A, Host Contract, Dominium Bridge, Workbench, Runtime, or
  Service;
- create branches or worktrees;
- mutate GitHub;
- release or promote.
