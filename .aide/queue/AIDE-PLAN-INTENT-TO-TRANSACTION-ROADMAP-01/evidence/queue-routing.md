# Queue Routing

Live queue truth wins over the source attachment and chat history.

The live source chain shows ContextPack v2 acceptance has already completed for
review with `ACCEPTED_WITH_WARNINGS` and recommends:

```text
AIDE-BUILD-INTEROP-EXPORTS-01
```

This planning task preserves that route. Future Host Contract, Capability,
Transaction, PreviewSession, Dominium Bridge, Workbench, and validation-slice
tasks are candidate follow-ons only.
