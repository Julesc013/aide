# Local frozen-handoff and broker transaction slice

Exact base: bfb86c12b9e6d2970024d29c57ba629994ec43cc on dev.
Task branch: task/aide-cw-integration-broker-01. No secondary worktree.

1. Inspect/noop/recover and run Git policy/detect/plan; classify preserved programme evidence and helper-generated reports. Record exact-base branch authorization.
2. Implement a bounded regular-file manifest, content blobs and reproducible Git tree; refuse links, escapes, protected changes, changed base, corrupt data and resource overruns.
3. Implement separate WAL/FULL broker reservations, unique target writers, durable apply intent, observed receipts and restart reconciliation. Production transport remains absent; CLI apply refuses.
4. Exercise real disposable local Git materialization plus explicitly injected transport observations, duplicate writers, lost responses, expiry, failed intent persistence and receipt mismatch.
5. Bind exact files and validation for independent review. Root exact-source review passed; current explicit programme authority authorizes the scoped local foundation checkpoint. No push in this checkpoint. Keep full broker, isolated host and real two-task qualification open.

The explicit user programme and current assignment authorize this bounded implementation and task activation. No additional scope or operational credential authority is inferred. The unchanged v0 coordinator remains disabled pending its separately reviewed v1 protocol adoption; this slice exposes a usable producer/broker API rather than silently changing frozen v0 activation semantics.

## Required next implementation under this same task

The reviewed foundation is not yet usable for the actual AIDE repository: its complete-tree handoff limit is 4,096 entries, while the current repository has 10,631 tracked files. Implement a bounded change-set handoff over trusted base Git objects; do not raise the entry budget or claim actual-repository readiness from small fixtures.

A process death after reservation but before prepared leaves a retained reserved writer. Add object-bound preparation recovery or an explicit safe repair operation, plus kill-before-prepared tests. Do not automatically delete an uncertain materialization directory, repeat remote apply, or treat lease expiry as ownership. Until then, interrupted preparation requires diagnosis and prevents unattended resumption.

Then wire coordinator-v1 frozen export/request/receipt adoption, with fresh tests and source assurance. These are authorized engineering work and are not new permission gates. Actual production transport and isolated-host qualification remain open.

## Source-reviewed checkpoint

Root reviewed exact source manifest 41633685e0cdbe1b4f33e2cf9a80620e304c229d9bcc7e24e4889c9f7b3456f8. Preserve original validation bytes in foundation-evidence.zip, with foundation-custody.json binding every member. Include the separately disclosed programme, ULK and USK receipt updates in the scoped checkpoint; they are integration observations, not broker test evidence. Continue the required same-task implementation after this checkpoint; it is not operational activation or full task closure.
