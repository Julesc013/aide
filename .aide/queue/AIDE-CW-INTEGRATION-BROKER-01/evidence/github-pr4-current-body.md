Recover exact worker candidates through a durable integration broker with finite attempts, write-ahead mutation intents and a registered provider bridge. The bridge binds immutable authority/store inputs, fences its Windows child and retains uncertain outcomes without replaying mutations.

The raw GitHub observer now checks actual repository, user, ref, commit, PR and Actions run/attempt/job records. It rejects ambiguous pagination and moved observations, derives merged ancestry from immutable Git objects, and keeps ordinary queued checks on the polling path. Read-only recovery has its own finite deadline after mutation authority expires.

Validation: independently reviewed source and raw custody; 26 observer tests, 21 existing PR/staged regressions, 43 registered-bridge tests, and 13 target graph/barrier experiments passed. The graph experiments are local evidence. [Current source and durable review records](https://github.com/Julesc013/aide/tree/57e28facc74c7407d4d270f5b0d21c0dc6e4ddbe/.aide/queue/AIDE-CW-INTEGRATION-BROKER-01/evidence).

The task remains in progress. The completion queue separately tracks an authenticated HTTP adapter, protected worker/credential/store isolation, actual restricted GitHub principal and target rules, hosted adversarial merge qualification, and the real two-task pilot. Activation remains disabled; target settings and credentials have not changed.

Work-Item: AIDE-CW-INTEGRATION-BROKER-01
