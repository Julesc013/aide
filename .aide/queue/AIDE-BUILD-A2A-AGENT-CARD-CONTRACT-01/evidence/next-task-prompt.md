# Next Task Prompt

Recommended next task:

```text
AIDE-CHECK-A2A-AGENT-CARD-CONTRACT-01
```

The next task should independently check the A2A contract-only slice without
modifying the implementation. It should verify source-chain integrity,
schema/helper alignment, agent-card shape, skill catalogue safety,
security/authentication boundaries, refusal mappings, deterministic projection,
source immutability, unsupported-operation fail-closed behavior, reports,
evidence, warnings, and explicit non-capabilities.
