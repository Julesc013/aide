# Next Task Prompt

Create `AIDE-LIFECYCLE-FIXTURE-INSTALL-MANAGED-SECTION-APPLY-01-RETRY` as the authorized retry of the first fixture apply proof. Verify this authority packet, verify the current preimage hash, run dry-run/report mode first if supported, execute exactly one `update_managed_section` mutation against `.aide/examples/apply/lifecycle-fixtures/target/existing-managed-section/manual/with-managed-section.md`, verify postimage hash, prove manual content preservation, record rollback-compatible evidence, prove no extra mutation, and stop at `needs_review`.
