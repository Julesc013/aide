# No-Apply Proof

This authority task did not execute:

- fixture apply;
- scoped transaction apply against fixture targets;
- lifecycle apply;
- install apply;
- rollback;
- uninstall;
- active repo apply;
- target repo mutation;
- branch/worktree mutation;
- merge, push, promotion, or release publication;
- GitHub mutation;
- provider/model calls;
- Gateway calls;
- network calls.

The only authorization created is for the named future task and named fixture target.
