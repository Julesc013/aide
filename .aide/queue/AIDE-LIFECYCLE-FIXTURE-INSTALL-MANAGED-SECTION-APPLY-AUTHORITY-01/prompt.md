# Prompt

Create a review-gated authority decision for exactly one future fixture-scoped managed-section apply proof for `install-managed-section`.

Do not execute apply in this task. Do not mutate fixture targets, active repo implementation files, target repositories, branch/worktree state, GitHub state, release state, provider/model/Gateway surfaces, or network state.
