# Next Batch

Selected next task: `AIDE-LIFECYCLE-FIXTURE-INSTALL-MANAGED-SECTION-APPLY-01-RETRY`

Goal: Retry the first fixture apply proof using the explicit authority packet in this task.

The retry must execute at most one fixture-scoped managed-section mutation and must stop at `needs_review`.
