# AIDE-APPLY-LIFECYCLE-PLAN-01 Prompt

Create a planning-only apply lifecycle plan after `AIDE-APPLY-02 - Scoped Transaction Executor v0` was accepted with notes. Define the proof ladder, prerequisites, blocked states, capability reality labels, evidence, validation, and next WorkUnit for install apply, upgrade apply, lifecycle repair apply, rollback apply, uninstall apply, fixture lifecycle apply, active AIDE repo apply gates, and target adoption gates.

Do not implement or execute lifecycle apply. Do not mutate target repositories, branches, worktrees, release state, GitHub, providers/models, Gateway, or network. End at `needs_review` with evidence.
