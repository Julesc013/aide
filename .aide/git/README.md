# AIDE Git Metadata

This directory stores repo-local Git governance templates and reports.

- `commit-template.md` defines the structured commit body expected for
  AIDE-managed work.
- Future Q28/Q29 phases will add branch workflow policy and helper reports.

Q27 does not install hooks automatically and does not mutate branches, remotes,
tags, releases, or GitHub settings.
