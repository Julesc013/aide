# WorkUnit CLI Unfinished Work

## Finished In This Slice

- Read-only `workunit status`.
- Read-only `workunit list`.
- Read-only `workunit inspect --task-id <TASK_ID>`.
- Read-only `workunit validate` with additive reports.

## Intentionally Deferred

- WorkUnit mutation CLI: intentionally deferred beyond the read-only WorkUnit CLI slice.
- workunit create: intentionally deferred beyond the read-only WorkUnit CLI slice.
- workunit claim: intentionally deferred beyond the read-only WorkUnit CLI slice.
- workunit run: intentionally deferred beyond the read-only WorkUnit CLI slice.
- workunit block: intentionally deferred beyond the read-only WorkUnit CLI slice.
- workunit finish: intentionally deferred beyond the read-only WorkUnit CLI slice.
- workunit repair: intentionally deferred beyond the read-only WorkUnit CLI slice.
- full WorkUnit runtime: intentionally deferred beyond the read-only WorkUnit CLI slice.
- scheduler: intentionally deferred beyond the read-only WorkUnit CLI slice.
- supervisor: intentionally deferred beyond the read-only WorkUnit CLI slice.
- WorkerRun schema: intentionally deferred beyond the read-only WorkUnit CLI slice.
- TestJob schema: intentionally deferred beyond the read-only WorkUnit CLI slice.
- Test Broker: intentionally deferred beyond the read-only WorkUnit CLI slice.
- Checkpoint schema: intentionally deferred beyond the read-only WorkUnit CLI slice.
- PromotionPolicy schema: intentionally deferred beyond the read-only WorkUnit CLI slice.
- branch/worktree allocator: intentionally deferred beyond the read-only WorkUnit CLI slice.
- Service: intentionally deferred beyond the read-only WorkUnit CLI slice.
- Commander: intentionally deferred beyond the read-only WorkUnit CLI slice.
- provider adapters: intentionally deferred beyond the read-only WorkUnit CLI slice.
- target repo apply: intentionally deferred beyond the read-only WorkUnit CLI slice.
- rollback execution: intentionally deferred beyond the read-only WorkUnit CLI slice.
- release/promotion: intentionally deferred beyond the read-only WorkUnit CLI slice.
- OpenTelemetry: intentionally deferred beyond the read-only WorkUnit CLI slice.
- SARIF: intentionally deferred beyond the read-only WorkUnit CLI slice.
- SPDX: intentionally deferred beyond the read-only WorkUnit CLI slice.
- CycloneDX: intentionally deferred beyond the read-only WorkUnit CLI slice.
- SLSA: intentionally deferred beyond the read-only WorkUnit CLI slice.
- in-toto: intentionally deferred beyond the read-only WorkUnit CLI slice.
- OpenAPI: intentionally deferred beyond the read-only WorkUnit CLI slice.
