# Warning Disposition

Acceptable warnings:

- The preferred `scripts/validate_public_alpha_readonly.py` command is absent in the pinned local Eureka checkout.
- The selected command reports `public_alpha_smoke.implicit_v0`, not `public_alpha_readonly_validation.v0`.
- State proof is limited to declared Git probe coverage.
- Provider remains proposed and unaccepted until the acceptance gate.
