# Warning Disposition

Retained warnings:

- Static interop exports are not accepted capability until acceptance review.
- MCP and A2A artifacts are previews only; no live server or endpoint exists.
- External tool-specific preview formats may need adjustment before any future
  installation task.
- Host Contract, CapabilityInvocation, Dominium Bridge conformance, Workbench,
  runtime, worker execution, provider/model/network calls, PatchTransaction
  apply, approval, admission, trust, release, promotion, and target mutation
  remain deferred.

These warnings are non-blocking for the check because the preview artifacts fail
closed and do not claim execution authority.
