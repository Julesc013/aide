# Required Field Review

Present: `name`, `description`, `version`, `capabilities`, `defaultInputModes`, `defaultOutputModes`, `skills`. Missing for the pinned A2A 1.0 shape: `supportedInterfaces`. Required values are otherwise non-null except provider URL and top-level URL placeholders discussed separately.
