# CLI Boundary Review

Required CLI commands remain status/project/validate only. Unsupported runtime operations are probed during validation and must fail closed: start, serve, register, publish, discover, send, delegate, submit, stream, subscribe, cancel, authenticate, and connect.
