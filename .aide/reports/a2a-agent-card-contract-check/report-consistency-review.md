# Report Consistency Review

Build reports agree on contract id `a2a-agent-card-contract-v0`, skill count 4, artifact count 6, no-runtime facts, and recommendation to this check. The check disagrees with the build's `contract_valid: true` only on independent external standards alignment, which the build explicitly deferred as full external schema validation warning debt.
