# Supported Interface Review

Material finding `A2A-CHECK-002`: `supportedInterfaces` is absent. The projected card therefore has no interface entry with `url`, `protocolBinding`, and `protocolVersion`. A top-level `url: null` cannot substitute for A2A 1.0 supported interfaces.
