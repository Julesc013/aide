# Contract Envelope Unfinished Work

## Finished In This Slice

- Minimal envelope builder and validator.
- Lifecycle fixture run, verify, and acceptance report projections.
- Additive validation reports under `.aide/reports/contract-envelope/`.

## Intentionally Deferred

- WorkUnit schema: intentionally deferred beyond the minimal envelope slice.
- EvidencePacket schema: intentionally deferred beyond the minimal envelope slice.
- TestJob schema: intentionally deferred beyond the minimal envelope slice.
- Checkpoint schema: intentionally deferred beyond the minimal envelope slice.
- PromotionPolicy schema: intentionally deferred beyond the minimal envelope slice.
- WorkUnit CLI: intentionally deferred beyond the minimal envelope slice.
- Test Broker: intentionally deferred beyond the minimal envelope slice.
- Service: intentionally deferred beyond the minimal envelope slice.
- Commander: intentionally deferred beyond the minimal envelope slice.
- provider adapters: intentionally deferred beyond the minimal envelope slice.
- branch/worktree allocator: intentionally deferred beyond the minimal envelope slice.
- target repo apply: intentionally deferred beyond the minimal envelope slice.
- rollback execution: intentionally deferred beyond the minimal envelope slice.
- release/promotion: intentionally deferred beyond the minimal envelope slice.
- OpenTelemetry: intentionally deferred beyond the minimal envelope slice.
- SARIF: intentionally deferred beyond the minimal envelope slice.
- SPDX: intentionally deferred beyond the minimal envelope slice.
- CycloneDX: intentionally deferred beyond the minimal envelope slice.
- SLSA: intentionally deferred beyond the minimal envelope slice.
- in-toto: intentionally deferred beyond the minimal envelope slice.
- OpenAPI: intentionally deferred beyond the minimal envelope slice.
