# Schema Helper Review

The schema parses and declares `kind: ContextPack`.

The helper projects one deterministic record with stable identity:

```text
aide://context-pack/minimal-context-pack-v2-01
```

The helper/schema subset agrees on envelope fields, source refs, section shape,
required capability/evidence/conformance refs, explicit non-capabilities, and
no-execution status facts.
