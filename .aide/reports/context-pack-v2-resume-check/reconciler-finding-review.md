# Reconciler Finding Review

The pack references `.aide/reports/reconciler/validation.json` as a source
surface. It does not run a Reconciler repair, mutate source truth, or resolve
inherited warning debt.
