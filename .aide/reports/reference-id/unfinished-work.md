# ReferenceID Unfinished Work

## Finished In This Slice

- Stable `aide://<kind>/<id>` identity parsing and formatting.
- Additive reference map projection over accepted protocol artifacts.
- Local validation and reports under `.aide/reports/reference-id/`.

## Not Attempted By Design

- event_record_implementation: intentionally deferred beyond the minimal Reference ID Scheme slice.
- okf_knowledge_bundle: intentionally deferred beyond the minimal Reference ID Scheme slice.
- reconciler: intentionally deferred beyond the minimal Reference ID Scheme slice.
- capability_manifest: intentionally deferred beyond the minimal Reference ID Scheme slice.
- conformance_profile: intentionally deferred beyond the minimal Reference ID Scheme slice.
- patch_transaction: intentionally deferred beyond the minimal Reference ID Scheme slice.
- adapter_manifest: intentionally deferred beyond the minimal Reference ID Scheme slice.
- context_pack_v2: intentionally deferred beyond the minimal Reference ID Scheme slice.
- runtime_reference_registry: intentionally deferred beyond the minimal Reference ID Scheme slice.
- resolver_service: intentionally deferred beyond the minimal Reference ID Scheme slice.
- database_state: intentionally deferred beyond the minimal Reference ID Scheme slice.
- leases: intentionally deferred beyond the minimal Reference ID Scheme slice.
- scheduler: intentionally deferred beyond the minimal Reference ID Scheme slice.
- supervisor: intentionally deferred beyond the minimal Reference ID Scheme slice.
- test_broker_runtime: intentionally deferred beyond the minimal Reference ID Scheme slice.
- async_execution: intentionally deferred beyond the minimal Reference ID Scheme slice.
- worker_execution: intentionally deferred beyond the minimal Reference ID Scheme slice.
- service: intentionally deferred beyond the minimal Reference ID Scheme slice.
- commander: intentionally deferred beyond the minimal Reference ID Scheme slice.
- provider_adapters: intentionally deferred beyond the minimal Reference ID Scheme slice.
- branch_worktree_automation: intentionally deferred beyond the minimal Reference ID Scheme slice.
- target_apply: intentionally deferred beyond the minimal Reference ID Scheme slice.
- active_apply: intentionally deferred beyond the minimal Reference ID Scheme slice.
- rollback_execution: intentionally deferred beyond the minimal Reference ID Scheme slice.
- uninstall_execution: intentionally deferred beyond the minimal Reference ID Scheme slice.
- release: intentionally deferred beyond the minimal Reference ID Scheme slice.
- promotion: intentionally deferred beyond the minimal Reference ID Scheme slice.
- github_mutation: intentionally deferred beyond the minimal Reference ID Scheme slice.
- gateway_calls: intentionally deferred beyond the minimal Reference ID Scheme slice.
- network_calls: intentionally deferred beyond the minimal Reference ID Scheme slice.
- model_provider_calls: intentionally deferred beyond the minimal Reference ID Scheme slice.
- production_readiness: intentionally deferred beyond the minimal Reference ID Scheme slice.
- release_readiness: intentionally deferred beyond the minimal Reference ID Scheme slice.
- broad_autonomous_runtime: intentionally deferred beyond the minimal Reference ID Scheme slice.
