# Warning Disposition

Accepted warnings:

- The trust and authorization contract is projection-only.
- No live enforcement, identity provider, credentials, policy engine, grants,
  Service, transaction approval, provider/model calls, network behavior, or
  mutation authority is accepted.

These warnings are retained as explicit scope boundaries.
