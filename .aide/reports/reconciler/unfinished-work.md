# Reconciler Unfinished Work

## Finished In This Slice

- Deterministic report-only drift finding model.
- Finding taxonomy for queue, evidence, protocol, ReferenceID, EventRecord, OKF, and capability overclaim drift.
- Local JSON and Markdown reports under `.aide/reports/reconciler/`.

## Not Attempted By Design

- drift_repair: intentionally deferred beyond the report-only Reconciler slice.
- source_truth_mutation: intentionally deferred beyond the report-only Reconciler slice.
- queue_acceptance_mutation: intentionally deferred beyond the report-only Reconciler slice.
- latest_task_packet_rewrite: intentionally deferred beyond the report-only Reconciler slice.
- okf_projection_refresh: intentionally deferred beyond the report-only Reconciler slice.
- protocol_report_rewrite: intentionally deferred beyond the report-only Reconciler slice.
- reference_id_rewrite: intentionally deferred beyond the report-only Reconciler slice.
- event_record_rewrite: intentionally deferred beyond the report-only Reconciler slice.
- capability_manifest: intentionally deferred beyond the report-only Reconciler slice.
- conformance_profile: intentionally deferred beyond the report-only Reconciler slice.
- patch_transaction: intentionally deferred beyond the report-only Reconciler slice.
- adapter_manifest: intentionally deferred beyond the report-only Reconciler slice.
- context_pack_v2: intentionally deferred beyond the report-only Reconciler slice.
- runtime_reconciler_service: intentionally deferred beyond the report-only Reconciler slice.
- scheduler: intentionally deferred beyond the report-only Reconciler slice.
- leases: intentionally deferred beyond the report-only Reconciler slice.
- supervisor: intentionally deferred beyond the report-only Reconciler slice.
- test_broker_runtime: intentionally deferred beyond the report-only Reconciler slice.
- async_execution: intentionally deferred beyond the report-only Reconciler slice.
- worker_execution: intentionally deferred beyond the report-only Reconciler slice.
- service: intentionally deferred beyond the report-only Reconciler slice.
- commander: intentionally deferred beyond the report-only Reconciler slice.
- provider_adapters: intentionally deferred beyond the report-only Reconciler slice.
- branch_worktree_automation: intentionally deferred beyond the report-only Reconciler slice.
- target_apply: intentionally deferred beyond the report-only Reconciler slice.
- active_apply: intentionally deferred beyond the report-only Reconciler slice.
- rollback_execution: intentionally deferred beyond the report-only Reconciler slice.
- uninstall_execution: intentionally deferred beyond the report-only Reconciler slice.
- release: intentionally deferred beyond the report-only Reconciler slice.
- promotion: intentionally deferred beyond the report-only Reconciler slice.
- github_mutation: intentionally deferred beyond the report-only Reconciler slice.
- gateway_calls: intentionally deferred beyond the report-only Reconciler slice.
- network_calls: intentionally deferred beyond the report-only Reconciler slice.
- model_provider_calls: intentionally deferred beyond the report-only Reconciler slice.
- production_readiness: intentionally deferred beyond the report-only Reconciler slice.
- release_readiness: intentionally deferred beyond the report-only Reconciler slice.
- broad_autonomous_runtime: intentionally deferred beyond the report-only Reconciler slice.
