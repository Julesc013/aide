# Warnings

- OwnershipLedger v1 is not install truth.
- OwnershipLedger v1 is not apply authority.
- Q43 migration is projection-only.
- Downstream objects must not infer vendor ownership from absent paths.
- Real target projects and canaries remain blocked until later reviewed tasks.
