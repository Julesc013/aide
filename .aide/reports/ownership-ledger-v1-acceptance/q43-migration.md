# Q43 Migration Acceptance

Q43 ownership migration is accepted as deterministic projection evidence only.

Known Q43 classes map to exact v1 classes; `unknown` requires manual review;
unmapped classes fail closed with `ownership.migration_unmapped`.

This does not authorize migration apply or target mutation.
