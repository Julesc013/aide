# Artifact Integrity Review

Status: `NOT_ACCEPTED_BLOCKED`

The independent check did confirm that the sample artifact digest recomputed to:

```text
sha256:5747bd0d486a73c1b363b0f4c8af974b4ee1f24968a53221eba2c89f187b3c5f
```

It also confirmed that changed artifact bytes change the digest.

That artifact-integrity evidence is preserved, but it is not accepted as part
of a capability because the acceptance gate is blocked by material path-scope
findings.
