# Next Task Prompt

Create and process `AIDE-ACCEPT-DISTRIBUTION-PRODUCT-STATUS-PROJECTION-01`.

Repo truth outranks this prompt.

Accept only the checked `distribution_product_status_v0` operator projection if the build and check tasks remain complete with zero material findings and zero missing evidence. Do not modify implementation, projection behavior, focused tests, fixtures, release/package artifacts, target repositories, canary inventory, provider/model/network behavior, branches, worktrees, or push state.

Expected result: `ACCEPTED_WITH_WARNINGS`.

Recommended next task after acceptance: `AIDE-BUILD-CANARY-PROFILE-SCREENSAVE-01`.
