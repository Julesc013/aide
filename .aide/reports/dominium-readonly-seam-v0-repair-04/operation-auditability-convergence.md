# Operation Auditability Convergence

Operation aggregation now preserves target, classification, source, allowed state, operation, family, and observation method; guard reports are produced through `exercise_guard`.
