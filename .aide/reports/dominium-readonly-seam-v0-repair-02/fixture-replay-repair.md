# Fixture Replay Repair

Negative fixtures now replay bounded JSON operations only. Executable operation fields are refused, and tests require every expected error code to be observed rather than accepting any overlapping error.
