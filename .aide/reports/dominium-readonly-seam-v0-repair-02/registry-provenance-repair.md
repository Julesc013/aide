# Registry Provenance Repair

Diagnostic and refusal registry projection summaries now include `path`, `source_registry_path`, `source_registry_sha256`, full `source_registry_git_object` metadata, `source_revision`, selection policy, counts, projected IDs, omitted ID digests, and truncation disclosure.
