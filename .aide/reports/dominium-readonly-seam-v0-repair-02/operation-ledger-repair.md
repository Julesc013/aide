# Operation Ledger Repair

The demo operation ledger records `allowed_operation_count`, `forbidden_operation_count`, observed operations, required operation families, family descriptions, and instrumentation methods for Git reads, filesystem writes, branch/worktree/ref operations, network attempts, provider/model attempts, worker dispatch, and mutation/apply boundaries.
