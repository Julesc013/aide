# Non-Capabilities

This check does not create or accept authority for:

- real target apply;
- AIDE source repo self-apply;
- target repository scanning;
- install/update/migration/rollback/repair/uninstall apply against a real target;
- ScreenSave, Eureka, or Dominium canary readiness;
- external repository mutation;
- release artifact generation;
- public release readiness;
- tags, uploads, or GitHub Releases;
- provider/model/network calls;
- push;
- branch/worktree automation.

The only recommended next task is `AIDE-ACCEPT-AIDE-SELF-CONSUMER-FIXTURE-V0-01`.
