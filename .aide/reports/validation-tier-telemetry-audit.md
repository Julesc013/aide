# Validation Tier Telemetry Audit

Result: PASS_WITH_WARNINGS.

X-TEST-00 validation tiers, impacted-test policy, telemetry schemas, examples, reports, and export-pack entries are present. `test tiers`, `test tier-plan`, `test impact-plan`, `test telemetry-status`, `test summary-validate --file`, `test full-discovery-handoff --reason`, and `test slow-report-validate --file` passed.

`test plan` is not an exposed exact subcommand; `tier-plan` and `impact-plan` are the supported planners.
