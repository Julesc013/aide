# Finding Disposition

- `capability_label.overclaims_observed_boundary`: `OPEN` - The build proposes a label that can be read as successful live validation, while the observed proof is a registered command-boundary invocation that returned a typed refusal.
