# Next Task Prompt

```text
# AIDE-BUILD-DOMINIUM-READONLY-SEAM-V0-01

Create and process AIDE-BUILD-DOMINIUM-READONLY-SEAM-V0-01.

Use .aide/queue/index.yaml as canonical AIDE queue truth.

Build only the minimal read-only AIDE-Dominium seam v0 authorized by
AIDE-ACCEPT-DOMINIUM-INTEGRATION-CHARTER-01.

Scope is limited to HostManifest, HostCapabilitySet, WorkspaceDescriptor,
ContextDescriptor, ArtifactReference, EventEnvelope, Diagnostic projection,
Refusal projection, Evidence refs, DominiumBridgeManifest, mapping fixtures,
and conformance expectations.

Do not invoke Dominium commands, mutate Dominium, implement runtime/service,
call providers/models/network, execute workers, preview/apply/rollback changes,
materialize scene workflows, create branches/worktrees, mutate GitHub, release,
or promote anything.

Materialize the queue packet, evidence, reports, validation, and next CHECK
prompt. Stop at needs_review.
```
