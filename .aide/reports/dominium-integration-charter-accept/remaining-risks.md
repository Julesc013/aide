# Remaining Risks

- The charter is planning-only and still needs a real read-only seam build/check/accept chain.
- Dominium local `main` remains behind the verified current remote object.
- Future Host Contract, Bridge, Workbench, trust, preview, apply, rollback, and scene workflows need separate queue authority.
- No live cross-repository command, service, provider, worker, or network behavior has been proven.
