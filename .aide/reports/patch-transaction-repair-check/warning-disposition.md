# Warning Disposition

Non-blocking warnings:

- No case-folding path policy is accepted.
- Full JSON Schema Draft implementation is absent.
- General diff parsing is absent.
- Artifact resolution and VCS reachability are absent.
- Policy evaluation, approval, apply, rollback, runtime, adapter execution,
  admission, and trust are absent.
- Inherited report, generated-output, OKF, and queue readability debt remains
  outside the repair-check scope.

None of these warnings reopens the two repaired path-scope defects.
