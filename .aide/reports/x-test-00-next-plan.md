# X-TEST-00 Next Plan

Immediate next task:

`X-TEST-01 - Eureka Tiered / Impacted / Timed Test Validation`

Then:

- `X-TEST-03 - Dominium Tiered Validation / CTest / RepoX Plan`
- `X-TEST-02 - AIDE Async Test Telemetry Summary Schema` if more telemetry detail is needed beyond v0 contracts
- `X-OS-00 - AIDE Task OS Schemas and Policies`

Task OS remains report-only/dry-run-only until validation tiers, transaction safety, branch provenance, and rollback semantics are proven.

