# EvidencePacket Unfinished Work

## Finished In This Slice

- Minimal EvidencePacket helper and validator.
- Additive projections from accepted lifecycle fixture and contract-envelope artifacts.
- Additive validation reports under `.aide/reports/evidence-packet/`.

## Intentionally Deferred

- full evidence engine: intentionally deferred beyond the minimal EvidencePacket schema slice.
- EvidenceStore: intentionally deferred beyond the minimal EvidencePacket schema slice.
- WorkUnit schema: intentionally deferred beyond the minimal EvidencePacket schema slice.
- WorkUnit CLI: intentionally deferred beyond the minimal EvidencePacket schema slice.
- TestJob schema: intentionally deferred beyond the minimal EvidencePacket schema slice.
- Test Broker: intentionally deferred beyond the minimal EvidencePacket schema slice.
- Checkpoint schema: intentionally deferred beyond the minimal EvidencePacket schema slice.
- PromotionPolicy schema: intentionally deferred beyond the minimal EvidencePacket schema slice.
- Service: intentionally deferred beyond the minimal EvidencePacket schema slice.
- Commander: intentionally deferred beyond the minimal EvidencePacket schema slice.
- provider adapters: intentionally deferred beyond the minimal EvidencePacket schema slice.
- branch/worktree allocator: intentionally deferred beyond the minimal EvidencePacket schema slice.
- target repo apply: intentionally deferred beyond the minimal EvidencePacket schema slice.
- rollback execution: intentionally deferred beyond the minimal EvidencePacket schema slice.
- release/promotion: intentionally deferred beyond the minimal EvidencePacket schema slice.
- OpenTelemetry: intentionally deferred beyond the minimal EvidencePacket schema slice.
- SARIF: intentionally deferred beyond the minimal EvidencePacket schema slice.
- SPDX: intentionally deferred beyond the minimal EvidencePacket schema slice.
- CycloneDX: intentionally deferred beyond the minimal EvidencePacket schema slice.
- SLSA: intentionally deferred beyond the minimal EvidencePacket schema slice.
- in-toto: intentionally deferred beyond the minimal EvidencePacket schema slice.
- OpenAPI: intentionally deferred beyond the minimal EvidencePacket schema slice.
