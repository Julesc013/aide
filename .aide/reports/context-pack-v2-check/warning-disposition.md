# Warning Disposition

The task result is `BLOCKED`, not `PASS_WITH_WARNINGS`.

Deferred capabilities remain unimplemented and unchecked:

- embeddings
- semantic retrieval
- exact provider tokenization
- agent execution
- provider selection
- runtime memory
- automatic freshness services
- adapter admission
- patch application

The warnings are not accepted as check pass conditions.
