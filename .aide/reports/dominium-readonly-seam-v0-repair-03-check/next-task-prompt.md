# AIDE-BUILD-DOMINIUM-READONLY-SEAM-V0-REPAIR-04

Repair only the material findings from `AIDE-CHECK-DOMINIUM-READONLY-SEAM-V0-REPAIR-03`.

- `schema.kind_specific_defs_present`: Public schema has kind-specific record/spec definitions and allOf/oneOf discrimination markers.
- `schema.authority_extensions_bounded`: Unbounded objects are limited to extension/compatibility surfaces and authority-changing extension names are semantically refused.
- `fixture.production_requires_value`: Production replay must require value for add, replace, and append rather than inserting null.
- `fixture.production_rejects_unicode_decimal_indexes`: Production replay must accept only ASCII canonical decimal indexes.
- `fixture.production_forbidden_key_set_complete`: Production replay must reject import, args, and kwargs operation keys in addition to executable fields.
- `conformance.unsupported_uses_actual_cli_dispatch`: Unsupported-operation conformance must consume actual CLI dispatch evidence, not direct helper output.
- `conformance.no_write_surrounds_actual_operations`: No-write conformance must compare Dominium state around actual seam operations.
- `conformance.guard_evidence_is_exercised`: Network/provider/worker/mutation conformance must consume exercised guard evidence.
- `operation.aggregate_key_preserves_semantics`: Operation aggregation key must preserve target, classification, and source distinctions or be explicitly lossless.
- `operation.guard_report_is_not_static`: Guard report must be generated by exercised guard paths, not static pass dictionaries.
- `portability.required_child_output_set_complete`: Portability proof compares the complete required child output set.
- `cli.unsupported_verbs_typed_refusal`: All required and arbitrary unsupported dominium-seam verbs return typed refusal through actual CLI parsing.

Do not broaden beyond the offline read-only seam.
