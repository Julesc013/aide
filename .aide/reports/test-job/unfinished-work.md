# TestJob Unfinished Work

## Finished In This Slice

- Minimal envelope-backed TestJob helper and validator.
- Additive metadata-only projections from accepted validation/check/acceptance artifacts.
- Additive validation reports under `.aide/reports/test-job/`.

## Not Attempted By Design

- Test Broker runtime: intentionally deferred beyond the minimal TestJob schema slice.
- async test execution: intentionally deferred beyond the minimal TestJob schema slice.
- real TestJob submission: intentionally deferred beyond the minimal TestJob schema slice.
- test-job run/retry/summarize runtime: intentionally deferred beyond the minimal TestJob schema slice.
- scheduler: intentionally deferred beyond the minimal TestJob schema slice.
- leases: intentionally deferred beyond the minimal TestJob schema slice.
- supervisor: intentionally deferred beyond the minimal TestJob schema slice.
- worker execution: intentionally deferred beyond the minimal TestJob schema slice.
- WorkUnit claim/run/finish/repair: intentionally deferred beyond the minimal TestJob schema slice.
- Service: intentionally deferred beyond the minimal TestJob schema slice.
- Commander: intentionally deferred beyond the minimal TestJob schema slice.
- provider adapters: intentionally deferred beyond the minimal TestJob schema slice.
- branch/worktree automation: intentionally deferred beyond the minimal TestJob schema slice.
- target repo apply: intentionally deferred beyond the minimal TestJob schema slice.
- active repo apply: intentionally deferred beyond the minimal TestJob schema slice.
- rollback execution: intentionally deferred beyond the minimal TestJob schema slice.
- release/promotion: intentionally deferred beyond the minimal TestJob schema slice.
- GitHub mutation: intentionally deferred beyond the minimal TestJob schema slice.
- Gateway: intentionally deferred beyond the minimal TestJob schema slice.
- network: intentionally deferred beyond the minimal TestJob schema slice.
- model/provider calls: intentionally deferred beyond the minimal TestJob schema slice.
