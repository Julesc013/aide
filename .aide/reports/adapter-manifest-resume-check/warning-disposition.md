# Warning Disposition

Warnings are non-blocking because the AdapterManifest slice fails closed and
does not claim admission, trust, execution, credential, network, provider,
GitHub, patch-apply, target-mutation, or runtime behavior.

The check result is `PASS_WITH_WARNINGS`.
