# Reference Review

Independent regex-based inspection checked 8 `aide://` references in the
generated manifest:

- manifest ref uses `artifact`;
- adapter ref uses `adapter`;
- required capability refs use `capability`;
- required ConformanceResult refs use `conformance-result`;
- required evidence refs use `evidence`.

ConformanceResult presence does not set `trusted: true`.
