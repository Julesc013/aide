# AIDE Token Savings Summary

Status: scaffolded by Q14 before ledger command generation.

This report is generated from estimated metadata only. It must not contain raw prompts, raw responses, provider credentials, local caches, or `.aide.local` state.

Run:

```bash
py -3 .aide/scripts/aide_lite.py ledger scan
py -3 .aide/scripts/aide_lite.py ledger report
py -3 .aide/scripts/aide_lite.py ledger compare --baseline root_history_baseline --file .aide/context/latest-task-packet.md
```

Q14 will regenerate this file with current packet sizes, budget status, baseline comparisons, regression warnings, and uncertainty notes.
