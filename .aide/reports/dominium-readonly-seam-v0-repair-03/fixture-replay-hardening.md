# Fixture Replay Hardening

The fixture replay engine now enforces strict add/remove/replace/append behavior, canonical non-negative array indexes, dash restrictions, non-root targets, pointer escape validation, and executable metadata rejection.
