# Schema Hardening

Repair 03 adds explicit kind-specific spec properties, allOf-based record/spec binding, bounded extension containers, and a complete false-boundary status contract. Authority-changing extension names are refused by semantic validation.
