# Genericity Scan

Result: `PASS`

The generic provider sources contain no Dominium-specific task IDs, capability
IDs, report paths, refusal codes, source paths, repository URLs, or local
absolute paths.
