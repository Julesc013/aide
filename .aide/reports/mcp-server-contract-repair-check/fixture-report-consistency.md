# Fixture Report Consistency

The fixture index contains 15 fixtures, matching the fixture directory.

Each fixture index SHA-256 value was independently recomputed from current
fixture bytes and matched the report.

Counts remain:

- resources: `10`
- tools: `7`
- prompts: `0`
- fixtures: `15`

The repaired resource-not-found code and pagination omission agree across the
fixtures and repair/check reports.
