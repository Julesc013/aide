# Streamable HTTP Expectation Review

The static Streamable HTTP profile records:

- a single endpoint expectation
- POST and GET expectations
- content-type and protocol-version header expectations
- session, resumability, cancellation, Origin validation, localhost default,
  and authentication expectations

Implementation status is `not_implemented`.

No URL is represented as live, no socket or port exists, no HTTP request
occurred, no SSE stream exists, and no session ID was created.
