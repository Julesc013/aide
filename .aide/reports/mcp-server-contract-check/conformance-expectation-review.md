# Conformance Expectation Review

Conformance expectation count: `22`

IDs are unique. Each expectation is marked `declared_not_executed` with
`fixture_or_static_projection_only` evidence status.

The expectation catalogue does not claim live server proof. It covers JSON-RPC,
versioning, lifecycle, capability negotiation, resources, tools, prompts,
transports, authorization, secrets, mutation exclusion, authority preservation,
determinism, and immutability.
