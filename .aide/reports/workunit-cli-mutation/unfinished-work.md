# WorkUnit CLI Mutation Unfinished Work

## Finished In This Slice

- `workunit create` from bounded JSON spec, with explicit dry-run/apply.
- `workunit block` for queue metadata blocking, with explicit dry-run/apply.
- `workunit evidence add` for safe evidence pointer metadata, with explicit dry-run/apply.

## Not Attempted By Design

- workunit claim: intentionally deferred beyond the queue metadata mutation CLI slice.
- workunit run: intentionally deferred beyond the queue metadata mutation CLI slice.
- workunit finish: intentionally deferred beyond the queue metadata mutation CLI slice.
- workunit repair: intentionally deferred beyond the queue metadata mutation CLI slice.
- full WorkUnit runtime: intentionally deferred beyond the queue metadata mutation CLI slice.
- worker leases: intentionally deferred beyond the queue metadata mutation CLI slice.
- scheduler: intentionally deferred beyond the queue metadata mutation CLI slice.
- supervisor: intentionally deferred beyond the queue metadata mutation CLI slice.
- WorkerRun schema: intentionally deferred beyond the queue metadata mutation CLI slice.
- TestJob schema: intentionally deferred beyond the queue metadata mutation CLI slice.
- Test Broker: intentionally deferred beyond the queue metadata mutation CLI slice.
- Checkpoint schema: intentionally deferred beyond the queue metadata mutation CLI slice.
- PromotionPolicy schema: intentionally deferred beyond the queue metadata mutation CLI slice.
- branch/worktree allocator: intentionally deferred beyond the queue metadata mutation CLI slice.
- Service: intentionally deferred beyond the queue metadata mutation CLI slice.
- Commander: intentionally deferred beyond the queue metadata mutation CLI slice.
- provider adapters: intentionally deferred beyond the queue metadata mutation CLI slice.
- target repo apply: intentionally deferred beyond the queue metadata mutation CLI slice.
- rollback execution: intentionally deferred beyond the queue metadata mutation CLI slice.
- release/promotion: intentionally deferred beyond the queue metadata mutation CLI slice.
- OpenTelemetry: intentionally deferred beyond the queue metadata mutation CLI slice.
- SARIF: intentionally deferred beyond the queue metadata mutation CLI slice.
- SPDX: intentionally deferred beyond the queue metadata mutation CLI slice.
- CycloneDX: intentionally deferred beyond the queue metadata mutation CLI slice.
- SLSA: intentionally deferred beyond the queue metadata mutation CLI slice.
- in-toto: intentionally deferred beyond the queue metadata mutation CLI slice.
- OpenAPI: intentionally deferred beyond the queue metadata mutation CLI slice.
