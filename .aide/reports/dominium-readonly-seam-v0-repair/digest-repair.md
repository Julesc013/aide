# Digest Repair

The repair adds shared integrity helpers for source snapshot, record, projection-index, and bundle self-digests. The bundle is finalized after validation metadata is embedded, and validation recomputes every digest path.
