# Identity Repair

The seam now parses Git remote URLs into host, owner, repository, transport, and canonical identity. The snapshot reader requires exact `github.com/julesc013/dominium` identity and rejects substring lookalikes.
