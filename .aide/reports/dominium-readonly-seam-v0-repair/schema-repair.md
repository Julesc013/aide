# Schema Repair

The public schema now requires the core bundle sections, shared metadata, source snapshot fields, projection summaries, digest sections, and exact record containers. The schema remains Draft 2020-12 and uses shared `$defs`.
