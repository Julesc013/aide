# Warning Disposition

Warnings retained as non-blocking:

- full vendored official A2A JSON Schema validation remains deferred;
- A2A remains contract-only and fixture-only;
- no endpoint, publication, registration, authentication, authorization, task delegation, worker execution, provider/model/network call, runtime, host integration, or mutation exists;
- this check does not accept the capability.

These are accurate capability boundaries, not material defects.
