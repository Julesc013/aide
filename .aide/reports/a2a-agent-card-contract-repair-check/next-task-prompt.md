# Next Task Prompt

# AIDE-ACCEPT-A2A-AGENT-CARD-CONTRACT-01
# Acceptance Review for Minimal A2A Agent Card Contract

Use `.aide/queue/index.yaml` as canonical queue truth. Re-read the live repository before writing anything.

Review the complete A2A build, failed independent check, bounded repair, and independent repair-check chain.

If all gates remain satisfied, accept only the minimal contract-only A2A Agent Card projection capability. Preserve that no live endpoint, publication, registration, authentication, authorization, task delegation, worker execution, provider/model/network call, runtime, host integration, repository mutation, release, promotion, or production readiness exists.

Stop at `needs_review` and recommend the live queue's next serialized task.
