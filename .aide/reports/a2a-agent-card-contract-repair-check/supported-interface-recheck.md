# Supported Interface Recheck

PASS.

The projected fixture interface is:

```json
{
  "url": "https://aide.invalid/a2a/v1",
  "protocolBinding": "JSONRPC",
  "protocolVersion": "1.0"
}
```

The `.invalid` URL is explicitly non-live and does not claim an endpoint exists.
