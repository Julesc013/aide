# Capability Recheck

PASS.

The official AgentCard capabilities object contains only accepted fields for this fixture subset:

- `streaming: false`;
- `pushNotifications: false`;
- `extendedAgentCard: false`.

The unsupported `stateTransitionHistory` field is absent.
