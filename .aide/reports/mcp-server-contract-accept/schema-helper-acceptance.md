# Schema Helper Acceptance

The schema parses and the helper output validates under the implemented structural subset.

Accepted alignment:

- envelope fields are present;
- contract identity is stable;
- target MCP version is `2025-11-25`;
- JSON-RPC version is `2.0`;
- runtime facts are explicit;
- explicit non-capabilities are preserved.

Full official MCP schema validation remains warning debt.
