# Conformance Expectation Review

Accepted facts:

- conformance expectation count: `22`;
- IDs are unique;
- expectations are `declared_not_executed`;
- evidence status is fixture/static projection only.

Acceptance of this catalogue does not mean a live MCP implementation has passed those expectations.
