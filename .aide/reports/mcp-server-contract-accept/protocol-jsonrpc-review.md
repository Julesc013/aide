# Protocol And JSON-RPC Review

Accepted facts:

- target protocol version: `2025-11-25`;
- JSON-RPC version: `2.0`;
- fixture count: `15`;
- fixtures parse;
- request, notification, result, and error shapes remain structurally valid;
- notification fixture remains ID-free;
- error codes are integers and error messages are strings.

The acceptance does not claim support for latest, all MCP versions, or full official schema conformance.
