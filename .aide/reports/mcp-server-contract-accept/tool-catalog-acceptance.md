# Tool Catalog Acceptance

Accepted tool catalogue facts:

- tool count: `7`;
- tool names are unique;
- input schemas are object-shaped JSON Schema definitions;
- every tool is `callable: false`;
- every tool has `execution_status: not_implemented`;
- every tool remains read-only or report-only.

No tools/list or tools/call runtime is accepted.
