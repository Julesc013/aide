# Prompt Catalog Acceptance

Accepted prompt catalogue facts:

- prompt count: `0`;
- empty prompt catalogue is structurally valid;
- prompt list fixtures parse;
- prompt pagination obeys the repaired omission rule.

No prompt serving, prompt model submission, tool invocation, WorkUnit creation, or mutation behavior is accepted.
