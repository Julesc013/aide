# Lifecycle Capability Review

The contract represents initialization, operation after initialized notification, and shutdown expectation.

Capability facts:

- resources catalogue projected;
- tools catalogue projected;
- prompts catalogue projected;
- callable tools: `0`;
- live endpoint count: `0`;
- implemented transport count: `0`.

No lifecycle engine or live MCP connection is accepted.
