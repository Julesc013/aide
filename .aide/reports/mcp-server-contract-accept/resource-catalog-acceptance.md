# Resource Catalog Acceptance

Accepted resource catalogue facts:

- resource count: `10`;
- resource names and URIs are unique;
- resources map to accepted AIDE object families;
- resources declare MIME type, source kind, authority role, sensitivity, and freshness behavior;
- resources remain `not_served_contract_only`.

No resources/list, resources/read, subscription, or serving runtime is accepted.
