# Version Pin Repair

Added explicit outer AIDE contract fields `target_a2a_specification_release: "1.0.0"` and `target_a2a_protocol_version: "1.0"`. AIDE contract version and agent fixture version remain distinct.
