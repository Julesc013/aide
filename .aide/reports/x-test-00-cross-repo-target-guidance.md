# X-TEST-00 Cross-Repo Target Guidance

Eureka should adopt this model through `X-TEST-01 - Eureka Tiered / Impacted / Timed Test Validation`.

Dominium should adopt this model through `X-TEST-03 - Dominium Tiered Validation / CTest / RepoX Plan`.

Target validators, target manifests, target timing records, and target product evidence remain target-local. X-TEST-00 did not mutate Dominium or Eureka and did not run target test suites.

