# Queue Authority Review

The previews preserve queue authority:

- `AGENTS.md.preview` states that it does not replace live `AGENTS.md` or
  `.aide/queue/` truth.
- `CLAUDE.md.preview` states that live AIDE queue records must be used before
  acting.
- `copilot-instructions.md.preview` states that generated reports are evidence
  and not a substitute for canonical queue truth.
- `aider.conf.yml.preview` is explicitly preview-only and not an active model
  configuration.
- MCP and A2A previews include inactive endpoint/server fields.

Preview files remain generated projections, not independent editable authority.
