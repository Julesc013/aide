# Agent Instruction Preview Review

`AGENTS.md.preview`:

- reflects task and evidence discipline;
- does not claim to be installed;
- does not grant network, provider, mutation, or runtime authority;
- does not supersede live `AGENTS.md`;
- preserves explicit non-capabilities.

`CLAUDE.md.preview`:

- is static generated guidance only;
- does not imply Claude Code is installed or available;
- contains no credentials;
- does not grant model/provider execution;
- does not claim live MCP access.

`copilot-instructions.md.preview`:

- is not installed into `.github/`;
- does not imply GitHub Copilot is configured;
- does not trigger GitHub mutation;
- preserves queue/evidence boundaries.

`aider.conf.yml.preview`:

- is not installed as live Aider configuration;
- contains no API keys or environment-derived secrets found by static scan;
- does not claim provider selection;
- does not automatically enable shell commands.
