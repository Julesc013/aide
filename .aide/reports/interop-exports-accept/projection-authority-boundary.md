# Projection Authority Boundary

Authority remains layered as:

```text
AIDE protocol / policy / queue = source authority
Interop preview files = generated projection
Manifest = generated inventory and integrity record
Build/check/accept reports = review evidence
```

Accepted AIDE inputs may regenerate preview artifacts in a future reviewed
projection flow.

Manually edited preview files do not become AIDE authority.
