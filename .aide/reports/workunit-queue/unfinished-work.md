# WorkUnit Queue Unfinished Work

## Finished In This Slice

- Minimal envelope-backed WorkUnit helper and validator.
- Additive projections from selected filesystem queue tasks.
- Additive validation reports under `.aide/reports/workunit-queue/`.

## Intentionally Deferred

- WorkUnit create/list/claim/block/finish/repair CLI: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- full WorkUnit runtime: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- scheduler: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- supervisor: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- WorkerRun: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- TestJob schema: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- Test Broker: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- Checkpoint: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- PromotionPolicy: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- branch/worktree allocator: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- Service: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- Commander: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- provider adapters: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- target repo apply: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- active repo apply: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- rollback execution: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- release/promotion: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- OpenTelemetry: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- SARIF: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- SPDX: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- CycloneDX: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- SLSA: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- in-toto: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
- OpenAPI: intentionally deferred beyond the minimal WorkUnit queue v1 slice.
