schema_version: aide.adapters-catalog.v0
profile_contract_version: aide.profile-contract.v0
catalog_status: metadata-only
source_of_truth_note: Adapter records describe target agent families. Q03 does not implement provider/model integrations or generated downstream artifacts.
adapters:
  - id: codex
    display_name: Codex
    status: metadata-only
    adapter_kind: agent-workflow
    generated_artifacts: planned-for-Q05
    implementation: not_implemented
    notes: Current work is prompt-driven through the filesystem queue; no Codex adapter implementation is added in Q03.
  - id: claude-code
    display_name: Claude Code
    status: metadata-only
    adapter_kind: agent-workflow
    generated_artifacts: planned-for-Q05
    implementation: not_implemented
    notes: Future generated guidance or adapter metadata may be considered in Q05 or later.
  - id: openhands
    display_name: OpenHands
    status: metadata-only
    adapter_kind: agent-workflow
    generated_artifacts: planned-for-Q05
    implementation: not_implemented
    notes: Reference target only; no integration is added in Q03.
  - id: generic-agents
    display_name: Generic agents
    status: metadata-only
    adapter_kind: agent-workflow
    generated_artifacts: planned-for-Q05
    implementation: not_implemented
    notes: Portable queue and ExecPlan behavior should remain provider-neutral.
rules:
  - Adapters must not own durable AIDE semantics.
  - Generated downstream artifacts remain outputs until Q05 defines deterministic boundaries.
  - Provider/model integrations are out of scope for Q03.
