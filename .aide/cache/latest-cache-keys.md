# AIDE Latest Cache Keys

Q18 placeholder. Run:

```text
py -3 .aide/scripts/aide_lite.py cache report
```

after cache commands are implemented.

This committed report is metadata-only. It contains no raw prompts, raw
responses, provider credentials, local traces, cache blobs, embeddings, vector
indexes, or `.aide.local/` contents.
