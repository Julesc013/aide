# AIDE Roadmap And TODO Synthesis Intake

## Status

ADVISORY_PLANNING_EVIDENCE_ONLY.

This document records the operator's July 2, 2026 roadmap/TODO update as a
bounded AIDE intake artifact. It synthesizes the update against the current
local queue, branch, and evidence surfaces after the earlier origin sync.

This is not an accepted roadmap, queue mutation, implementation task, target
install, release claim, provider call, branch operation, or public publication
plan. The AIDE intent compiler classified this prompt as `release` risk,
`blocked`, and `safe_to_execute: false`; the correct action is an advisory
blocker/synthesis report that requires reviewed authorization before mutation.

## Source Update Summary

The operator update makes the right product-level correction:

```text
Do not treat the synthesis as one roadmap.
Treat it as synchronized product programs.
```

The durable product definition to preserve is:

```text
AIDE is a portable, local-first, evidence-native development control plane
and living project twin for proof-native projects.
```

The core operating sequence to preserve is:

```text
repair truth freshness
-> restore validation
-> use read-only real canaries
-> build ProjectGraph
-> conserve work products
-> introduce transaction-backed mutation
-> only then apply, package, publish, or automate
```

The strongest laws in the update are:

```text
Universal semantics at boundaries.
Domain meaning inside domains.
Host presentation inside hosts.
Specialized execution in hot paths.

Models propose.
Policy authorizes.
Domains apply.
Evidence proves.

ProjectGraph models.
OKF explains.
Protocol executes.
Evidence proves.

No useful work disappears without a recorded disposition.
No reusable work remains invisible.
No failed work teaches nothing.
No task closes without harvest evidence.
```

These laws match AIDE's current repo doctrine: AIDE should coordinate work
identity, context, policy, grants, evidence, events, transactions, project
intelligence, installation safety, and interoperability without stealing domain
truth from ScreenSave, Dominium, Eureka, Carbon, More Infinite Research, or
future target projects.

## Current Local Truth Check

Fresh local inspection on July 2, 2026 found:

- `git fetch origin main` completed.
- `git status --short --branch` reported `main...origin/main [ahead 1]`.
- `git rev-list --left-right --count origin/main...HEAD` reported `0 1`.
- Local `HEAD` is `5c8e4238 docs(intake): reapply synthesis after origin sync`.
- `origin/main` is `e4a519f8 audit(queue): plan project intelligence spine`.
- The one local-only commit is the prior reapplied advisory synthesis commit;
  origin is not ahead at this check.

The current task-routing surfaces are not perfectly aligned:

- `.aide/context/latest-task-packet.md` names
  `AIDE-CHECK-SELF-MANAGEMENT-CHARTER-01`.
- `py -3 .aide/scripts/aide_lite.py task inspect` without an explicit task id
  resolves `AIDE-PLAN-PROJECT-INTELLIGENCE-SPINE-01`.
- `py -3 .aide/scripts/aide_lite.py task inspect --task-id AIDE-CHECK-SELF-MANAGEMENT-CHARTER-01`
  reports that task complete at `needs_review` with `missing_evidence: 0`.
- `.aide/queue/AIDE-ACCEPT-SELF-MANAGEMENT-CHARTER-01/status.yaml` already
  records `result: ACCEPTED_WITH_WARNINGS`, `accepted_baseline: true`, and
  `recommended_next_task: AIDE-BUILD-DOC-KNOWLEDGE-TRUTH-RECONCILER-01`.

Therefore the first current TODO is not to execute the pasted roadmap. It is
to reconcile the task-routing projection so the latest task packet, default
task inspector, queue statuses, and operator-facing next-task guidance point to
the same reviewed truth.

## Correction To The July 1 Framing

The July 1 synthesis treated Q49-style truth-surface repair as the local
starting point and treated local trust enforcement as likely next on public
main. The current local checkout is materially ahead of that framing.

Verified queue and log evidence now show:

- `local_service_foundation_v0` was accepted.
- `local_trust_enforcement_v0` was built, checked, and accepted.
- `durable_local_worker_run_slice_v0` was built, repaired, checked, and
  accepted.
- the distribution update protocol, manifest, project lock, ownership ledger,
  install record, migration record, update plan, rollback bundle, update
  receipt, distribution apply engine, self-consumer fixture, routing repair,
  and distribution product-status projection have all moved through local
  queue surfaces to `needs_review` states.
- the More Infinite Research canary profile exists and is `PARTIAL`, because
  it was public-metadata-only and no configured local target checkout was
  found.
- the ultimate synthesis roadmap and Project Intelligence planning spine
  already seeded pending queue tasks for MIR inputs, Structure Intelligence,
  ProjectGraph, semantic records, health observers, ProjectGraph ContextPack,
  Workbench views, safe refactoring, distribution package/shadow canaries,
  read-only interop, preview/native planning, and future requirements intake.

This means the update is directionally right, but several "next" tasks in the
text are now historical or already processed locally. The live rule should be:

```text
Use the roadmap as product strategy.
Use the queue as execution truth.
Refresh stale routing surfaces before acting.
```

## Four Synchronized Product Programs

The best refinement is to hold four programs in parallel, with queue gates
deciding when each can advance.

### Program 1: Trust And Local Runtime

Purpose:

```text
make AIDE locally durable and authorization-aware before broader runtime work
```

Current local truth:

- `local_service_foundation_v0` is accepted with warnings.
- `local_trust_enforcement_v0` is accepted with warnings.
- `durable_local_worker_run_slice_v0` is accepted with warnings.
- These accepted surfaces remain narrow, deterministic, local, fixture-backed,
  and non-autonomous.

Still not accepted:

- external identity;
- secrets or credential management;
- OIDC/IAM or remote policy;
- general worker harness;
- autonomous AI worker runtime;
- persistent daemon;
- live MCP/Workbench runtime;
- provider/model/network execution;
- preview/apply/rollback authority.

Best next shape:

```text
refresh task routing
-> follow current reviewed queue gate
-> keep local runtime additions narrow and evidence-backed
-> require explicit queue authorization before broader worker/runtime work
```

### Program 2: AIDE Lite Distribution, Update Safety, And Canaries

Purpose:

```text
make AIDE installable, updateable, rollbackable, and safe across real projects
without overwriting target truth
```

Current local truth:

- DistributionManifest, ProjectLock, OwnershipLedger, InstallRecord,
  MigrationRecord, UpdatePlan, RollbackBundle, UpdateReceipt, DistributionApply
  Engine, self-consumer fixture, and product-status projection queue surfaces
  have already been processed into review-gated states.
- Distribution product-status is accepted as a read-only projection, not as
  apply capability or release readiness.
- MIR is now the operator-prioritized canary target, but the existing MIR
  profile is partial because it lacks a local target checkout.

Key TODO:

```text
configure or declare the local MIR target checkout
-> process AIDE-BUILD-CANARY-PROFILE-MORE-INFINITE-RESEARCH-INPUTS-01
-> check and accept the MIR canary profile only after local inputs exist
-> keep all canary work read-only until a later reviewed transaction/apply gate
```

Canary non-goals remain:

- no MIR mutation;
- no package generation unless separately authorized;
- no Factorio mod portal upload;
- no branch/worktree automation;
- no provider/model calls;
- no release publication.

### Program 3: ProjectGraph And Work-Product Conservation

Purpose:

```text
make AIDE understand project structure and conserve useful work before it
attempts structural mutation
```

Current local truth:

- `AIDE-PLAN-PROJECT-INTELLIGENCE-SPINE-01` is complete at `needs_review`.
- ProjectGraph foundation, semantic records, health observers,
  ProjectGraph-driven ContextPack, Workbench views, and safe refactoring/scale
  mode tasks exist as pending tentative queue surfaces.
- Structure Intelligence tasks also exist as pending tentative queue surfaces.

The best synthesis is to combine ProjectGraph and harvest instead of treating
them as unrelated work:

```text
ProjectGraph records what exists and what it means.
WorkProductLedger records what work created, learned, and left behind.
TaskHarvest turns evidence, scripts, fixtures, failures, and prompts into
classified reusable knowledge or explicit discard decisions.
```

Minimum ProjectGraph objects to carry forward:

- `ResourceRef`
- `ProjectGraphSnapshot`
- `PathAuthorityRecord`
- `OwnershipRecord`
- `SymbolRecord`
- `DocClaimRecord`
- `PurposeRecord`
- `ContractRecord`
- `UsageRecord`
- `ReuseOpportunityFinding`
- `ComplexityFinding`
- `PerformanceObservation`

Minimum work-product conservation objects:

- `WorkProductLedger`
- `TaskHarvestReport`
- `ReuseRegistry`
- `TestAssetCatalog`
- `ScriptLifecycleLedger`
- `FixtureCatalog`
- `PromptTemplateCatalog`

Every substantial task close should answer:

- What did this task create?
- What did it learn?
- What failed?
- What can be reused?
- What should become a regression test?
- What should become a fixture?
- What should become docs or OKF material?
- What should remain task-local evidence?
- What should be discarded, and why?

### Program 4: Preview, Apply, Rollback, And Native Experience

Purpose:

```text
create the first safe path from evidence to real mutation, then project that
truth through CLI, Workbench, host adapters, and native surfaces
```

Required transaction objects:

- `DevelopmentTransaction`
- `StructureTransaction`
- `CodeStructureTransaction`
- `DocStructureTransaction`
- `ReferenceRewritePlan`
- `WorkProductPromotionTransaction`
- `PreviewSession`
- `ShadowWorkspace`
- `RollbackBundle`

Required transaction inputs:

- ProjectGraph slice;
- WorkProduct records;
- allowed and forbidden paths;
- ownership evidence;
- affected docs, tests, reports, symbols, and generated outputs;
- validation plan;
- rollback plan;
- human review decision.

Exit criteria for mutation:

```text
mutation is previewable
mutation is reversible
mutation is evidence-backed
mutation is review-gated
```

Native experience should wait for this truth substrate. Workbench, Commander,
IDE adapters, mobile views, and hosted surfaces should compress accepted truth,
not outrun it.

## Operator Dashboard Requirement

The update correctly identifies a missing product surface: AIDE has too much
truth and not enough compression.

The compact operator projection should separate:

- `task_gate_state`
- `validation_result`
- `acceptance_state`
- `capability_state`
- `runtime_state`
- `distribution_state`
- `structure_state`
- `harvest_state`

The dashboard should show:

- current executable gate;
- accepted capabilities;
- proposed capabilities;
- blocked chains;
- truth freshness;
- validation freshness;
- distribution readiness;
- canary status;
- structure state;
- work-product harvest backlog;
- next three queue tasks.

This should be built as a read-only projection first. It must not become a
second queue, second source of truth, or shortcut around review gates.

## Stability And Rationale Records

The update also identifies important future record types:

- `NameDecision`
- `PathDecision`
- API stability tiers;
- `PerformanceObservation`
- `ComplexityFinding`
- `BenchmarkRecord`
- `HotPathRecord`
- `ResourceBudget`

These should be ProjectGraph-adjacent records, not free-floating notes.

Suggested API stability tiers:

- `experimental`
- `reviewed_internal`
- `stable_public`
- `deprecated`
- `removed`
- `generated_private`
- `runtime_private`

Every exported command, schema, report, package component, public path, and
adapter-facing protocol should eventually carry a tier. That should be planned
and checked before being enforced.

## Best Immediate Plan

The strongest immediate plan is not:

```text
start a new runtime push
start Workbench
mutate MIR
publish AIDE Lite
refactor aide_lite.py
clean orphan files
manually replay old roadmap ids
```

The strongest immediate plan is:

1. Reconcile current task routing.
   - The latest task packet, default task inspector, explicit task statuses,
     and current queue recommendations disagree about what should be treated as
     current.
   - Produce or process a reviewed routing/status repair before acting from
     stale latest-packet text.

2. Preserve this update as advisory planning evidence.
   - Do not promote it directly to root roadmap law.
   - Do not mutate queue entries outside a reviewed queue-shaping task.
   - Do not treat chat synthesis as canonical execution truth.

3. Continue from the live queue, not July 1 assumptions.
   - Local trust enforcement is already accepted.
   - Durable WorkerRun slice is already accepted.
   - Distribution safety has advanced substantially.
   - MIR profile exists but is partial and needs local input resolution.
   - Project Intelligence and Structure Intelligence are pending queue waves.

4. Run the next reviewed queue gate after routing is fresh.
   - Current evidence points toward self-management/doc-truth/generated-output
     routing on one axis and MIR inputs / Project Intelligence queue waves on
     another.
   - The operator should choose only if the refreshed queue still presents
     multiple legitimate next gates.

5. Keep the four product programs synchronized.
   - Trust/runtime provides durable local safety.
   - Distribution/canaries proves practical downstream value.
   - ProjectGraph/harvest provides understanding and conservation.
   - Preview/apply/native experience provides safe mutation and product feel.

## Concrete TODO Backlog

### Routing And Truth Freshness

- Refresh the task-routing projection so latest packet, default inspect,
  explicit queue statuses, and root docs agree.
- Re-run broad validation after routing repair and record whether any export
  checksum drift remains.
- Keep the one local-only advisory synthesis commit visible until the operator
  decides whether to push, hold, or fold it into later queue work.

### Trust And Runtime

- Do not redo `local_trust_enforcement_v0`; it is already accepted locally.
- Treat `durable_local_worker_run_slice_v0` as accepted but narrow.
- Plan any broader worker/runtime work only through reviewed queue tasks.
- Preserve explicit non-capabilities around network, provider/model calls,
  persistent daemons, autonomous workers, and target mutation.

### Distribution And Canaries

- Resolve local MIR checkout inputs for the MIR canary.
- Process MIR inputs before MIR check/acceptance.
- Keep MIR as the first practical canary unless operator routing changes.
- Keep ScreenSave, Dominium, Eureka, and Carbon as later specialized canaries
  with their own domain truth and acceptance laws.
- Keep package source, shadow apply, self-update, and downstream canary waves
  report-only until apply authority is explicit.

### ProjectGraph And Structure Intelligence

- Process Structure Intelligence as report-only before ProjectGraph mutation
  inputs are enforced.
- Build ProjectGraph foundation as projection-only.
- Add naming/path authority records before durable structure creation.
- Link docs, tests, reports, symbols, generated outputs, and ownership in one
  graph model.

### Work-Product Conservation

- Plan WorkProductLedger and TaskHarvest as required closeout surfaces.
- Classify reusable scripts, tests, fixtures, prompts, reports, generated
  artifacts, failures, and observations.
- Add disposition reasons for archived, superseded, quarantined, and discarded
  work.
- Turn negative findings and failed attempts into regression or knowledge
  candidates when useful.

### Preview, Apply, And Rollback

- Require ProjectGraph and WorkProduct inputs for mutation proposals.
- Keep DevelopmentTransaction and StructureTransaction previewable first.
- Require rollback evidence before apply.
- Keep target projects authoritative for domain semantics and validation law.

### Native Experience

- Build read-only operator dashboard/status projections before interactive
  mutation surfaces.
- Let Workbench compress accepted truth first.
- Add Preview Canvas, Evidence Drawer, RepoGraph View, Structure Impact View,
  and Context Lens only as the underlying records become real.

## Blockers And Deferrals

Current blockers:

- Direct execution of the pasted roadmap is blocked by the intent compiler.
- Current task-routing surfaces need reconciliation before a single "next task"
  can be treated as authoritative.
- MIR local checkout inputs are unresolved; the current MIR profile is partial.
- Root roadmap/TODO mutation is not authorized by this intake.
- Target repository mutation is not authorized.
- Release, package publication, GitHub mutation, provider/model calls, and
  network execution remain unauthorized.

Current deferrals:

- No Workbench, Commander, mobile, IDE adapter, or native app surface work from
  this prompt.
- No ProjectGraph implementation from this prompt.
- No WorkProductLedger implementation from this prompt.
- No transaction/apply/rollback implementation from this prompt.
- No target install/update/apply work from this prompt.

## Final Recommendation

Freeze the product doctrine, not the stale sequence.

The best current plan is:

```text
refresh routing truth
-> preserve advisory roadmap update
-> follow the reviewed live queue
-> resolve MIR local inputs
-> advance report-only Structure Intelligence and ProjectGraph
-> add harvest/work-product conservation
-> require previewable reversible transactions before mutation
-> build native experience only over accepted truth
```

That preserves the real promise:

```text
AIDE improves projects without stealing their truth or destroying their history.
```
